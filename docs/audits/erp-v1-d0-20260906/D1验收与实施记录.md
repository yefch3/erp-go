# D1 验收与实施记录

执行时间：2026-09-06～07。本文件记录D1实际实施；D0报告和D1原方案中的“尚未实施”保留为当时历史，不代表本轮授权。用户已明确授权D1，不进入D2，不合并main。

## 工作区与业务依据

- 工作区：`C:\Users\16789\Documents\erp-go\.worktrees\erp-v1-d0-20260906`。
- 分支：`codex/erp-v1-d0-20260906`；基线 `87d73c4993382e881a53d593c3c42c6cb102c02c`。D1修改已提交到本独立分支，未合并main；当前提交以该分支`HEAD`为准。
- 原目录仍为 `codex/fix-finance-actions-responsive`，HEAD仍为上述基线；local main仍为 `cc797854286aebb5e20344bf363facf0325611aa`。两项未合main财务修复 `2fc65f6a`、`87d73c49` 均继承。
- 正式需求SHA256：`FE127EAAA0511434E5900FF19D403EC05CC01215237E6F6BA95346CEDA248E47`，原文未改。辅助清单只用于安排测试；D0所列旧§编号差异仍保留。D1未发现辅助清单反向业务规则；正式原文内部Q1～Q12未借D1擅自裁定。

## 已实施内容

1. 客户询盘手工保存、邮件转入、负责销售编辑、提交、撤回和重提。产品行与来源编号稳定；同一来源同时进入采购、物流及客户报价接收。状态仅未提交/询价中/已撤回。
2. 工厂报价按部分或全部产品填写。保存与提交分离，未提交仅作者可改，提交后有权限的部门同事可改，保留报价人与实际修改人。支持搜索、已报/未报筛选、20/50/100、跨页选项和Excel价格粘贴映射。
3. 货代与船公司分开；同货代多个方案、日期可空、整批费用与可选部分货物；小计decimal计算，多币种分别合计。页面输入预览使用BigInt，保存时后端重新计算。
4. 客户报价接收页只读查看已提交工厂/货代完整报价、附件及修改结果；客户询盘页不混入报价编辑。最终客户价公式、PDF新流程及合同业务留给D2。
5. 同库根行锁和轮次阻止旧轮报价写回；export同步失效栅栏与报价/合同触发器保护已确认/已成合同历史；本地客户确认也与撤回共用源行锁。故障不报告成功，重试幂等清理；这是两个本地事务，不宣称分布式原子提交。
6. 源附件和报价附件绑定tenant/case/轮次，撤回后旧报价附件不能重用；D1下载及旧询盘详情返回受登录权限保护的URL，复制URL不会授予新权限。
7. 真实来源待办随保存、提交报价、撤回、重提变化，仅跳转原业务；负责销售范围与只读上级区分。老板使用BOSS只读预置角色，已有及新租户均有对应定义。旧领取、主责、RFQ前置、经理方案、成本确认、重做等售前写RPC退出；旧发RFQ邮件、旧intake导入、成本转客户报价在网关先退出，避免副作用。
8. 历史方案不批量转成新已提交报价。仅有明确销售提交时间的旧采购/物流方案按冻结明细做只读展示；未有提交证据的不暴露给销售，不冒充新轮报价，不改变原引用和金额。历史货代/船公司合并字段保留原值，不猜拆分。

## 隔离环境

Docker项目 `erp-d1-20260906`；独立内部网络、项目卷、工作区构建的专属镜像。数据库只在内部网络，所有应用DSN指向该网络内 `postgres:5432/erp_*`。Postgres卷 `erp-d1-20260906_pgdata`。gateway及MinIO单独入口网络，端口只绑定loopback；业务数据库没有可用的主机端口。

测试页面 `http://127.0.0.1:25173`，网关 `http://localhost:28080`。前端Vite明确代理28080。外部邮件、OAuth和AI凭据为空；邮件源为D1夹具，没有发真实邮件，没有调用外部AI。Docker Desktop启动时既有配置为自动重启的容器可能随守护进程恢复，本批数据库操作均明确指向D1容器；没有查询或修改既有业务库。

迁移：IAM59、procurement61、export28；必要公共服务独立迁移并运行。另建 `erp_d1_upgrade`（D1样本副本）验证58→61；旧采购/物流方案、明细、客户选择校验值保持 `943783dc0f42608bc9b5a672ebb3e183`。IAM预置角色对照使用另一个一次性库。

## 实际验收矩阵

| 项 | 实际结果与证据 |
|---|---|
| A01 | 真实S1保存及编辑；源编号、产品ID保持；API和局部数据库测试通过。 |
| A02 | D1邮件1由S1转入询盘7；交付脚本复验为询盘24；联系人、附件ZIP、产品与港口保留；其他销售拒绝。未运行外部收信或AI识别。 |
| A03 | 三接收方从真实接口收到同3产品、同来源；无需领取或审批，通过。 |
| A04–A05 | 私存销售不可见、另一采购不能改；提交后P2可改，S1读取更新及修改人，通过。 |
| A06 | 126行保存、重开、提交、P2改第126项为98.76，销售一致；浏览器搜索126和100条分页/第二页26条通过；跨页/筛选粘贴映射单元测试通过。真实Excel剪贴板事件仍留人工验收。 |
| A07–A09 | 同货代两个方案、日期空、部分货物、L2协作改价、USD/CNY分算通过。UI在询盘16将20.05改20.15并保存，USD变60.45、CNY仍42.22，修改人L2。 |
| A10 | API撤回清除当前轮报价和未确认export报价；UI原文警告及无需原因已核验。 |
| A11 | 改数量为7后重提，采购/物流旧轮请求均409，旧报价附件403；通过。 |
| A12 | 真实来源17对应已接受报价11、来源18对应合同报价12，撤回均409，原记录保留。局部DB另覆盖本地客户确认/合同栅栏。 |
| A13 | 12并发报价与撤回、客户确认与撤回共锁通过；停D1 export后失败保留源，恢复重试成功。export提交后注入本地删除失败，报500，移除故障重试清理完成（来源19）。没有以一次只读检查冒充互斥。 |
| A14 | 真实S1/S2、P1/P2、L1/L2、经理M1、只读老板B1、无权U1和tenant22001的X：列表/详情/写API/文件拒绝或只读符合；复制下载URL也拒绝越权。 |
| A15 | 浏览器验证新询价“去处理”直达来源；采购报价提交后来源16不再出现在采购待报列表；重提来源19重新待报；撤回源离开部门列表，销售得到重提待办。客户最终报价后续完成状态属D2，未伪造完成。 |
| A16 | 旧参与写API实测409；旧售前写RPC已停用，涉及邮件/导出副作用的旧网关入口在调用前409；网关完整回归通过。 |
| A17 | 隔离历史采购与物流快照、租户隔离及迁移校验通过；export历史合同导入/原价保护/执行看板/PDF/收款/发运局部回归11项通过。没有连接真实历史业务库，也不声称验证了每一条真实存量记录。 |
| A18 | 1280×720真实浏览器检查销售提交/警告、采购全宽编辑、产品规格分列、分页、物流整批费用及保存；未把构建通过等同布局验收。更多电脑尺寸留用户验收。 |

原始脚本在 `d1-scripts/`，已保存日志在 `d1-evidence/`。专项Go测试的IAM/目录等替身仅记局部验证；真实账号链路另列，二者不混记。

## 技术检查

- 前端typecheck通过；生产build通过（1957模块，已有Rollup注释与大包提示）；39文件219测试通过。
- 网关完整测试通过；相关gRPC适配包编译通过（无测试文件不计业务验收）。
- procurement D1数据库并发/历史及旧合同收货、原询盘文件、付款凭证回归共5项通过；export D1及历史回归11项通过；IAM一次性新库预置角色对照测试通过（含BOSS）。
- protobuf按仓库版本再生成、buf lint通过；sqlc v1.31.1生成，真实新增模型为两个服务的D1表；既有查询无业务变化。
- 租户列、租户种子、IAM种子、迁移安全、重复路由、Compose变量检查已运行。第一次Alpine的邮件检查遇到BusyBox不支持`--include`，其“passed”不计有效结果，GNU邮件沙箱单项重跑通过；其余六项在Alpine的有效检查结果见日志。

## 用户验收步骤

1. 打开 `http://127.0.0.1:25173`，用 `s1@d1.example.test` 登录；测试密码 `D1-Acceptance-2026!`。新建3产品询盘，保存后换S2/P1/L1确认不可见有效询价，再由S1提交。
2. P1/P2（`p1@…`、`p2@…`）进入采购询价：部分产品先保存，S1客户报价看不到；提交后可见；P2修改已提交报价，再由S1核对修改人、时间、金额。
3. L1/L2进入物流询价：同货代建两方案、船名日期暂空，分别填USD和CNY整批费用。先保存再提交，由另一物流改价并由S1查看。
4. 126行现成数据是询盘ID16（客户名D1 126 products）。检查20/50/100、产品/规格搜索、已报/未报、跨页选产品；从Excel复制价格/日期/备注到对应产品的单价框，保存重开检查。
5. 使用新建未确认询盘测试撤回：保持采购和物流编辑页打开；S1更多操作撤回。核对警告、部门列表消失、旧页面保存失败；修改数量重提，必须重新报价。不要在确认/合同保护样本上期待撤回成功。
6. 来源17/18分别用于已确认与已有合同保护；S1撤回应被拒绝。B1（`b1@d1.example.test`）可查看但不能修改；S2/U1复制详情或附件链接应拒绝。
7. 待办只点“去处理”，操作完成来自业务源；记录浏览器尺寸及与预期不一致的步骤。

## 未完成与边界

- D1尚待用户人工验收，尤其真实Excel粘贴及不同屏幕尺寸。未把这些项记为已完成。
- 历史数据只用隔离样本核对；无明确提交证据的旧资料不自动映射成销售可见报价。上线前仍需在授权的历史副本上逐条审核异常映射。
- 目前附件上传技术上限3MB；大附件尚未实施分片/流式上传，应在正式使用前补充验证与处理，不将此限制写回正式需求。
- 全局待办筛选/已完成历史及D2以后的类型未在D1重做。D1接入真实询盘/源报价待办；最终客户报价完成、公式、新PDF、合同及后续实单均未进入本批。
- 跨服务故障是明确失败后重试，不具自动后台补偿；故障时export栅栏防止错误确认，重试已验证。未宣称跨库原子性。

本批记录更新后停止；没有推送、部署到业务环境或合并main。后续阶段及最终合并仍须用户明确指令。

## 收尾修复与复验

- IAM新库对照首次发现旧迁移的角色可能先于租户初始化记录存在，BOSS种子改为覆盖租户表与已有角色的tenant并集。新建第二个一次性IAM库完整迁移后，预置权限/范围对照通过；首次失败日志保留。
- 验收脚本不再用PowerShell遗留 `$LASTEXITCODE` 判定纯PowerShell环境检查；检查抛错直接终止。
- export故障测试的重启重连等待由8次300ms改为有界90次500ms，以覆盖gRPC重连退避；没有为测试修改撤回业务规则。
- UI物流修改验收的当前值为询盘16第一方案USD60.45；API批量验收原值USD60.15仍保留在原日志，二者发生时间不同。
- 原目录最终 `git status --porcelain` 为空，HEAD/main校验与上述基线一致；两个财务修复的祖先检查通过。
最终复验：D1 export恢复后撤回重试通过（询盘26），容器恢复running；浏览器L2保存的询盘16方案A，由真实S1接收接口读到USD60.45、CNY42.22及修改人L2。见 `failure-api.log`、`browser-to-sales.log`。本批至此停止，等待用户按上方步骤验收，不进入D2、不合并main。

## D1 UI 调整补记（2026-09-07）

用户要求改善当前 UI，本批仅修改独立 worktree 的前端展示与导航交互，未改数据库或服务端业务规则，未进入 D2、未合并 main。

- Shell：防止主导航被内容挤压；窗口宽度不超过 1000px 时通过“导航”按钮展开/收起，路由变化后收起。
- 邮箱：卡片、侧栏和阅读区统一间距；不超过 760px 时上下排列，修复邮箱登录框被双侧栏挤成竖排的问题。
- 询盘/采购/物流共用页面：增加标题说明、独立筛选区、状态标签、清晰表格行间距，右侧固定操作列；提交时间按浏览器本地时区显示到分钟。
- 销售询盘页：具有邮件读取权限时提供“前往邮件 · 转入询盘”导航入口，仍进入已有邮件流程，不宣称实现直接邮件导入或绕过解析。

验证实际结果：
- npm run typecheck：通过。
- npm run test -- src/lib/inquiryWorkspace.test.ts：4/4 通过。
- 浏览器约 624px 窄窗口：邮箱登录框恢复正常横排；询盘标题、筛选和分页可见；L2 物流列表加载 10 条，固定操作列可见，导航展开/收起正常。
- 当前浏览器会话为 L2，访问销售/邮件出现权限拒绝；未为 UI 验证扩大权限。销售账号下邮件快捷入口以及宽屏效果尚需人工复核。
- 本次未重跑全部后端跨模块回归；仅 UI 调整，既有 D1 未完成项仍有效。

验收：刷新页面；窄窗口点击左上角“导航”；使用 S1 查看客户询盘筛选、状态、详情及邮件快捷入口，使用 L1/L2 查看物流报价列表；核对邮箱登录区域是否正常显示。停止于本批 UI 调整，等待用户检查。

## D1 UI、邮箱权限与待办结构修订（2026-09-07）

本批根据用户逐项验收意见修订，仍在 `codex/erp-v1-d0-20260906` 独立 worktree；未进入 D2，未合并 main。

- 邮箱改为所有在职 ERP 员工的基础能力。导航不再按业务角色隐藏邮箱；IAM 权限判定及登录权限清单对在职员工固定补充 `mail:email:read`、`mail:email:write`，无业务角色员工也可使用本人邮箱，停用员工仍被拒绝。邮件数据仍按本人范围读取。
- 询盘与报价待办从页面标题下方移入“待我处理”标签内容，和采购补充任务、审批任务处于同一个待办区域；数量仍计入顶部“待我处理”汇总。
- 销售第一个子模块统一改为“客户询盘”，移除新页面的“销售工作台”字样；销售菜单新增“询盘模板”。
- 正式需求全文使用“外销合同”，前端合同菜单、页面标题、待办类型、单据类型及角色说明中的“出口合同”统一改为“外销合同”。现有 `/contracts` 是该计划模块，但 D1 只改正名称，正式需求页面 3 的完整外销合同流程仍属于后续阶段。
- 询盘模板管理已经存在并可修改 Excel 表头、顺序、必填、默认值、增加自定义列、创建版本及设置默认模板；入口返回客户询盘。它驱动邮件识别与询盘导入，当前手工新建询盘的产品表格仍使用正式需求固定字段，未扩写成动态表单。
- 销售、采购、物流共用询盘页面改为蓝绿色业务主题，增加模块头部、主操作区、客户与交付卡片、附件卡片、产品需求卡片，统一表格、输入框、按钮、文件选择器和移动端排版。

实际验证：

- `npm run typecheck`：通过。
- `npm run test`：39 个测试文件、219 项全部通过。
- `npm run build`：1957 个模块构建通过；保留既有第三方 PURE 注释和大包警告。
- `go test ./services/iam/internal/app`：通过（无 DSN 时集成项按约定跳过）。
- 在 D1 隔离网络和 `erp_iam` 数据库运行 `TestActiveEmployeeGetsPersonalMailWithoutRole`：通过；覆盖无角色在职员工读写邮箱权限，以及员工停用后的拒绝。
- 重建并替换 D1 专用 IAM 镜像后，真实 API 登录验证 S1 与无业务角色 U1 均获得邮箱读写权限。
- 浏览器实际验证 S1：左侧出现“邮箱”；询盘与报价 15 项位于“待我处理”标签内部；客户询盘页不显示“销售工作台”，显示“询盘模板”和“从邮箱转入询盘”；新建详情卡片布局正常；模板管理页可读取两个现有模板并显示编辑操作。

用户验收：刷新并重新登录后检查左侧邮箱；打开“我的待办 → 待我处理”确认询盘任务位于标签内；打开“销售 → 客户询盘”检查列表与新建详情；打开“销售 → 询盘模板”检查模板编辑入口；确认合同菜单显示“外销合同”。本批停止等待用户继续验收。

## D1 询盘产品表按模板动态显示（2026-09-07）

用户在看到固定产品表后明确新增要求：手工询盘产品明细也要随询盘模板改动。本节替代上一节“手工新建询盘仍使用固定字段”的边界说明；其他 D1 边界不变。

- 销售新建或编辑未提交/已撤回询盘时，可在产品需求卡片选择当前生效的询盘模板。表格按模板字段顺序和显示名即时变化，必填字段显示星号，NUMBER/DATE 使用对应输入控件，字段默认值在空单元格中自动带入。
- 单价、总价仍由报价阶段填写，在客户询盘中显示为只读提示，避免销售把采购报价写进客户原始需求。
- 询盘保存模板 ID、编码、版本和完整字段快照。管理员以后发布模板新版本不会改变历史询盘；已使用的历史模板版本仍可在原询盘中保留。
- 每行自定义字段与标准字段一起保存；切换模板只改变当前显示列，隐藏字段值不被删除。采购和物流读取同一份模板快照和产品字段。
- 服务端不信任浏览器传来的字段定义：按租户和模板 ID 重新读取真实版本，校验编码/版本，应用默认值，并在提交时校验模板必填字段、数字和日期类型。
- 邮件/Excel 转入形成的旧结构询盘在读取时按其原 `inquiry_template_id` 补齐模板快照，并从采购行标准字段及 `custom_fields` 恢复动态列。

实际验证：

- `npm run typecheck`：通过；`npm run test`：39 个测试文件、221 项全部通过；`npm run build`：1957 个模块构建通过，仅保留既有第三方注释和大包警告。
- `go test ./services/procurement/... ./services/gateway/...`：全部通过；新增模板必填、数字类型、默认值保护测试通过。
- D1 隔离采购镜像已重新构建并替换。浏览器用 S1 新建 `SC-20260908-000025`，从“钢材详细尺寸模板 v1”切换到“系统标准询盘模板 v1”，表头从 26 个模板字段变为 22 个模板字段。
- 在该询盘填写产品“热轧钢卷”、数量 `12.5`、单位 `MT` 和自定义“高度/边长2”值 `888`；保存、重启采购容器并重新打开后，模板版本、数量单位和自定义值均完整恢复。

用户验收：打开客户询盘并新建；在产品需求右侧切换两个模板，核对列名和顺序即时变化；编辑模板并保存为新版本后再新建询盘，核对新询盘使用新版本、旧询盘保持旧版本。当前页面已保留在隔离环境供验收。本批仍未进入 D2，未合并 main。

## D1 客户报价详情旧数据兼容修复（2026-09-07）

用户验收时发现客户报价列表可见但“打开详情”没有反应。复现确认详情接口为 200，权限正常；根因是部分早期 D1 样本的 `attachments` 为 JSON `null`，页面读取 `.length` 时中断渲染并留在旧列表画面。

- procurement 读取询盘时统一把产品、附件和每行自定义字段规范化为空数组/空对象，不再向新旧消费端返回 `null` 集合。
- 前端详情入口、直接链接、刷新、保存、提交、撤回和报价保存响应均增加同样的兼容规范化，避免旧服务或历史 JSON 再次触发空集合渲染异常。
- 浏览器实际点击 `SC-20260907-000017` 后成功进入详情并显示“暂无附件/暂无报价”；点击有报价的 `SC-20260907-000014` 后显示 1 份工厂报价、2 份货代报价，且“查看完整报价”弹窗可打开。
- 服务重建后 API 再验 `SC-20260907-000017`：HTTP 200，附件为 `Object[]`、数量 0，产品数量 1。
- `npm run typecheck`、`go test ./services/procurement/internal/app ./services/gateway/internal/httpapi` 通过；D1 procurement 镜像已重建并替换。未进入 D2，未合并 main。

## D1 询盘详情、报价阅读与基础资料联动修订（2026-09-07）

本批按用户对销售页面五项反馈调整询盘共用详情；同一组件供销售、客户报价、采购和物流使用，因此产品、附件和操作布局同步生效。业务状态、报价权限和D1边界未改变。

- 已提交报价改为分层摘要卡片。工厂报价直接显示已报价产品数、币种、统一交期、有效期以及前三条产品的名称、规格、需求量、单价和交期；其余产品按“另有N项”进入完整报价。货代报价直接显示多币种合计、船公司、运输方案、日期、港口和适用货物范围。报价人、提交时间、实际修改人和修改时间保留。
- 产品需求只读页面默认“精简视图”，显示关键列，每行可展开全部模板字段；可切换“全部字段”。表格最大高度520px，20/50/100分页和横向滚动保留，126产品不会继续拉长整页。销售编辑时仍显示模板全部字段，保证所有字段可填写。
- 客户附件取消大面积空状态，改成文件数量和紧凑文件按钮；宽屏时位于客户信息右侧并按内容高度显示，窄屏自动下移。
- 销售可按客户编码或名称检索基础数据，也可直接输入临时客户；选择客户后可选择联系人，联系人候选显示部门、职务、邮箱和主要联系人，空联系人自动带出主要联系人。询盘JSON和`sourcing_cases.customer_id/contact_id`同时保存关联ID，采购、物流和客户报价读取同一客户快照名称。
- 详情页去掉重复的页面大标题和独立返回/刷新条；提交询价或添加部门报价保留为主按钮，返回、刷新、保存草稿、撤回等按状态收入加粗并带阴影的“更多操作”。
- 询盘产品校验改成逐行提示，例如“第2行未填写单位”“第3行数量必须是大于0的数字”。用户截图中的 `SC-20260908-000026` 实际已保存产品“钢材”、数量`10`、单位`MT`并处于询价中，最终提交成功；旧提示无法指出具体行的问题已修正。

实际验证：

- 最终 `npm run typecheck`：通过；`npm run test -- --run`：39个测试文件、221项全部通过；`npm run build`：1957个模块构建通过，仅有既有第三方PURE注释和大包提示。
- `go test ./services/procurement/internal/app ./services/gateway/internal/httpapi`：通过；`git diff --check`：通过。
- D1 procurement镜像已从独立worktree重建并替换。浏览器实测询盘16的126个产品默认20条精简分页、逐行展开入口和全部字段切换均可见；客户报价显示1份工厂报价和2份货代报价，工厂报价显示126/126及前三条单价，货代显示USD/CNY合计、方案及修改人。
- 隔离主数据建立`CU-0001 / D1 客户资料联动验收`及主要联系人。真实S1浏览器输入“D1”出现客户候选，选择后自动带出主要联系人。另建询盘`SC-20260908-000027`验证body和数据库均保存`customer_id=1/contact_id=1`，验证后把该临时询盘标记为CANCELLED；客户夹具保留供用户验收。
- 宽屏浏览器检查附件卡片与客户信息两栏；报价摘要分区、更多操作菜单和固定高度产品区正常。未以构建通过代替页面检查。

用户验收步骤：

1. 使用S1打开客户询盘，进入`SC-20260908-000025`；在客户框输入“D1”，选择`D1 客户资料联动验收`，确认联系人自动变为`D1 主要联系人`。直接输入其他文字也应保留为临时客户/联系人。若要保留改动再点“更多操作 → 保存草稿”。
2. 打开客户报价中的`SC-20260907-000014`，核对工厂报价的126/126、前三条产品单价与交期；核对两份货代报价的CNY/USD合计与方案；点击“查看完整报价”。
3. 在同一询盘产品需求区确认默认20条、页面高度不会随126项无限增长；展开任一行查看全部模板字段，再切换“全部字段”和20/50/100分页。
4. 检查客户附件只占紧凑区域；将窗口缩窄后确认客户信息、附件和报价摘要正常换行。
5. 分别用P1和L1进入采购询价、物流询价，确认“添加工厂报价/添加货代报价”是主按钮，返回和刷新位于“更多操作”，产品和附件使用相同布局。
6. 新建询盘并增加第二行，只填写产品后提交，确认提示明确指出“第2行数量必须是大于0的数字”；填写数量但不填单位时应提示“第2行未填写单位”。

未完成项：仍等待用户对本批页面观感、不同屏幕和真实Excel粘贴进行人工验收；客户主数据当前只自动带入客户名称和联系人，客户的默认贸易条款、默认港口等字段是否自动带入需要正式需求给出映射规则，D1未擅自推断。D2的最终客户价、PDF、外销合同及后续实单没有进入本批。未合并main。

## D1 上传式新建、长列表与收尾验收修订（2026-09-07）

本节替代前文关于3MB附件、仅单元测试粘贴、D1待办历史和跨服务无自动补偿的旧状态。本批仍在独立worktree与`codex/erp-v1-d0-20260906`分支执行，没有进入D2，没有合并main。

- “上传或新建客户询盘”现在首先打开标准Excel/CSV导入窗口。销售可自动识别文件表头或指定生效模板、下载所选模板、填写询盘标题、从客户资料选择客户与联系人并自动带出联系邮箱，再上传文件进入网页复核。少量产品仍可选择手工录入。导入按模板显示名映射字段、应用默认值、拒绝空表、重复/未知表头和超过10000条的文件；原文件作为客户附件随询盘保存。
- 详情改为“概览与附件 / 产品需求 / 已提交报价”三个页签，一次只展示一类信息。126或更多产品继续使用固定高度、搜索和20/50/100分页；采购价格表和物流费用表也限制可视高度。多份报价改成一行一份的紧凑对比表，完整产品价格、费用与附件在弹窗内按固定高度查看。
- 当前D1询盘待办增加编号/客户/事项搜索及类型筛选；没有任何报价的询盘不再被列为“客户报价待处理”。“已处理”页增加D1客户询盘已提交、工厂报价已提交和货代报价已提交记录，并可按相同条件筛选。D2以后的单据类型仍按辅助清单要求随对应阶段接入，没有提前伪造来源。
- 客户与报价附件上限从3MB提高到8MB；网关HTTP、gRPC客户端、采购gRPC服务端及命令JSON预算按base64膨胀同步调整。隔离环境真实上传、写入询盘、重新下载7MB文件，7340032字节一致。
- export先变更可用性而采购本地事务随后失败时，采购服务使用不受请求取消影响的5秒恢复上下文自动反向补偿，最多重试3次。真实故障注入确认本地询盘与报价回滚、export撤回栅栏自动清除，之后原操作可正常重试。export在操作开始前完全不可用时仍明确返回失败并保持源数据，这是失败保护，不会把未执行动作显示成成功。
- 窄屏浏览器首次发现上传表单固定标签宽度把控件挤坏，随后改为上下排列的全宽表单并复验。约600px页面中模板、标题、客户、联系人、邮箱和文件控件均正常横向显示；桌面布局保持表格与页签结构。

实际结果：

- 前端`typecheck`通过；最终全量测试40个文件、223项全部通过；最终生产构建1958个模块通过，只保留既有第三方PURE注释和大包提示。
- procurement相关app、gRPC入站/出站测试通过；gateway HTTP API测试通过；`git diff --check`无空白错误（仅Windows换行提示）。
- 真实跨模块主链路再次通过：S1保存/提交，采购、物流和销售报价接收三方读取相同3项产品；采购私存销售不可见、提交后可见；撤回调用真实export并成功。
- 浏览器在询盘16的126产品采购报价页聚焦首行单价并发送真实paste事件，粘贴三行Tab分隔的价格/日期/备注；页面即时变为“已报价3/126”，三行分别显示101.25、202.50、303.75和对应日期、备注。没有保存这次浏览器临时测试值。
- 浏览器确认“已处理”页显示13条D1询盘处理记录、完成时间、状态、查看链接和独立筛选；确认新版上传窗口在窄屏修复后可用。当前浏览器已恢复S1并把上传窗口留给用户验收。

本批用户验收步骤：

1. 当前页面是`http://127.0.0.1:25173/sales/inquiries`的“上传标准询盘”窗口。选择模板或保留自动识别，选择`CU-0001 / D1 客户资料联动验收`及主要联系人，上传从“询盘模板”下载并填写的CSV/XLSX，确认导入后直接进入产品复核页且原文件出现在附件中。
2. 打开询盘16，分别检查产品需求和已提交报价页签；产品每页20条且区域固定，多份报价一行一份，点击“查看完整报价”再看产品或费用明细。
3. 使用P1进入询盘16，新增工厂报价，勾选第一项后从Excel复制三行“单价、交货日期、备注”粘贴到首行单价，确认自动勾选并填充连续三项。按需保存或直接返回，不会影响已有报价。
4. 打开“我的待办”，在“待我处理”和“已处理”中使用询盘搜索与类型筛选；确认没有报价的询盘不出现在客户报价待办，已提交询盘可从历史记录打开。
5. 如需复核大附件，上传不超过8MB的文件；现成隔离样本询盘35保存了7MB附件。超过8MB应明确拒绝。

仍需用户或外部材料的项目：用户最终页面观感和业务验收尚未完成；真实历史数据只能在用户提供经过授权、脱敏的数据库副本后执行逐条兼容核对。目前已通过D1隔离升级副本和合成历史样本，不能据此声称全部真实存量已验证。D2的最终客户价、报价PDF、外销合同和后续实单没有进入本批。

## D1 用户人工验收结论（2026-09-07）

用户已完成本轮 D1 页面与业务流程人工验收，并明确确认验收通过。至此，D1 约定范围内的代码实施、自动化测试、跨模块回归和用户人工验收均已完成。

真实历史数据兼容仍受外部材料限制：当前未取得经过授权、脱敏的现有业务数据库副本，因此只完成了隔离升级副本与合成历史样本验证。该项作为外部验证缺口保留，不判定为 D1 代码缺陷。

本记录更新后停止 D1 实施。未进入 D2，未合并 `main`；后续仅在用户明确指示后推送远程分支、创建合并请求或执行合并。

## D1 GitHub CI 收口（2026-09-07）

长期分支首次推送后，Draft PR #383 的 CI 在 `sqlc-check` 失败。业务测试尚未开始失败；原因是 `EmployeeHasPermission` 新增邮箱基础权限条件后仍使用位置参数，仓库锁定的 sqlc v1.31.1 无法为第 2、3 个参数稳定推断 `EmployeeID` 与 `Code`，同时提交的 IAM 生成文件漏掉了查询注释更新。

- IAM 权限查询改用 `sqlc.arg(tenant_id)`、`sqlc.arg(employee_id)` 和 `sqlc.arg(code)` 明确参数名称及类型。
- 使用 CI 相同的 sqlc v1.31.1 重新生成 IAM store；对外 Go 参数名保持 `TenantID / EmployeeID / Code`，业务调用无需改变。
- 第二轮 CI 通过生成检查后，在后续 lint 发现 export 集成测试的事务清理未显式处理 `Rollback` 返回值；同步修正 procurement 中同一写法，防止流水线在下一模块再次停止。两处只显式忽略提交后必然出现的 `ErrTxClosed` 清理结果，不改变测试业务路径。
- 本机 `go test ./services/iam/...`、export/procurement 相关应用层测试全部通过；使用 CI 锁定的 golangci-lint v1.62.2 分别扫描 export 和 procurement 应用层，均无问题。重新推送后以 GitHub #383 CI 结果作为最终收口依据。

该修订只解决 D1 生成代码一致性，不扩展业务范围；未进入 D2，未合并 `main`。
