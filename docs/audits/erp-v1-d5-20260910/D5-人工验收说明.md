# D5 工厂出货前质检：人工验收说明

## 验收基线与边界

- 起点：长期分支 `codex/erp-v1-rebuild-20260906` 的 D4 验收提交 `2cbc8b51`。
- D5 分支：`codex/erp-v1-d5-20260910`；独立 worktree：`.worktrees/erp-v1-d5-20260910`。
- 本次新增的是独立的“工厂出货前质检”任务，不复用采购收货后的质检记录。
- 质检申请是采购与质检部门之间的业务交接，不增加审批。
- 本阶段不修改宣船、船名航次、订舱号、提单号、运输提醒、五公式输入口径和物流费用分摊。
- 质检结果通知采购、采购端查看质检资料及部分合格先发流程暂列待确认项，不作为本次 D5 验收和合并条件。

## 验收环境与账号

- 前端：<http://127.0.0.1:25375>
- 网关：<http://127.0.0.1:28282>
- 所有验收账号密码：`D5-Acceptance-2026!`

| 身份 | 账号 | 验证重点 |
| --- | --- | --- |
| 采购 P1 | `p1@d5.example.test` | 在采购订单中分批申请质检；不显示质检模块 |
| 质检 Q1 | `q1@d5.example.test` | 逐产品结论、附件、复检和完整历史 |
| 物流 L1 | `l1@d5.example.test` | 不显示质检模块，也不能通过链接或接口进入 |
| 相关销售 S1 | `s1@d5.example.test` | 不显示质检模块，也不能通过链接或接口进入 |
| 上级 B1 | `b1@d5.example.test` | 不显示质检模块，也不能通过链接或接口进入 |

验收采购单为 `PO-D5-ACCEPT-001`，产品为冷轧卷，采购数量 `10 MT`。

## 建议验收步骤

1. 使用 P1 登录，进入“采购管理 → 采购订单”，在 `PO-D5-ACCEPT-001` 的“更多操作”中点击“申请质检”。填写本次送检数量 `6 MT`、计划日期、地点、联系人和备注后提交。确认该操作不出现审批步骤。
2. 再次尝试对同一产品提交超过剩余数量的批次，页面应提示累计送检数量不能超过采购数量；合法的剩余批次仍可稍后申请。
3. 使用 Q1 登录，打开独立“质检”模块。顶部应只有“待质检”和“已完成”两个标签。在“待质检”中直接打开任务并记录本轮结果。
4. 第一轮把该产品记录为“部分合格”，合格 `4 MT`、不合格/待复检 `2 MT`，填写异常说明、处理建议和备注。分别上传现场照片或视频、质检报告、第三方报告及其他附件中的若干项并提交。
5. 确认任务仍留在“待质检”，同一任务内显示第一轮完整结论、附件和时间/操作人历史；随后在同一任务发起下一轮，将剩余 `2 MT` 记录为合格并上传复检资料。
6. 确认该任务移入“已完成”，逐产品累计合格数量为 `6 MT`，两轮历史和每轮附件均可重新打开或下载。
7. 分别使用 P1、L1、S1、B1 登录，确认左侧不显示“质检”，直接访问质检地址会被权限守卫拦截，调用质检任务读取接口返回 HTTP 403。
8. 使用 P1 回到采购订单，确认仍可申请新的送检批次；本阶段不验收质检结果通知、采购端资料入口及部分合格先发处理。
9. 回到 P1，再对该采购单申请剩余 `4 MT`。确认系统允许形成第二个分批任务；随后再次申请任意正数应被累计数量上限拦截。

## 数据与权限口径

- 质检任务、现场资料和完整历史仅质检部门可访问；采购、物流、销售和上级不授予 `quality:task:read`。
- 采购保留申请质检的权限，不开放质检页面。
- `PASS`、`PARTIAL`、`FAIL` 只能由质检角色写入。
- 每轮现场资料保存文件元数据、分类、所属任务/轮次和对象存储键；打开资料时生成新的受控下载地址。
- 异常不会创建游离的新任务，而是在原任务中追加复检轮次，保留完整审计历史。

## 自动验证记录

- 数据库迁移：采购、IAM 的 D5 migration 已在独立数据库执行；采购迁移通过 Up → Down → Up。
- D5 核心集成测试覆盖：分批累计上限、部分合格、附件登记/再次读取、同任务复检、最终完成和完成后结论不可改；现有放行数据能力不作为本次交付的采购业务入口。
- 前端：类型检查通过；Vitest `56` 个文件、`349` 项测试通过；生产构建通过。
- 协议：Buf lint 通过，本阶段没有修改 protobuf；Windows 本地重生成只产生注释格式差异，已丢弃该环境噪声，最终 `proto-check` 由 GitHub Linux CI 在干净工作区复核。
- 数据访问代码：sqlc 生成一致性检查补齐了 D5 质检表对应的采购模型；其余生成文件没有业务差异。
- 全仓 Go：`pkg`、`gen` 与 approval/export/fx/gateway/iam/inventory/mail/masterdata/procurement/product/shipping 全部 `go test ./...` 通过。
- 全仓 lint：上述 Go 模块按 CI 固定的 golangci-lint `v1.62.2` 顺序执行并全部通过；期间修正了 D5 质检事务回滚返回值未显式处理的问题，采购测试随后再次通过。
- 静态守卫：tenant id、tenant seed、IAM seed、迁移安全、邮件沙箱、重复路由和 Compose 必填变量检查全部通过；D5 两个新增 migration 的授权按角色所属 tenant 写入且没有 tenant 1 默认数据。
- 真实账号 API 冒烟：P1 申请 6 MT；Q1 第一轮部分合格 4 MT、待复检 2 MT；报告实际上传对象存储并重新读取；Q1 在同任务第二轮复检 2 MT 后完成。最终两轮、合格 6 MT、附件 1 个。
- 权限和数量：Q1 读取质检列表返回 HTTP 200；P1/L1/S1/B1 均无 `quality:task:read` 且读取接口返回 HTTP 403；P1 仍保留采购订单中的申请质检权限；累计超采购量申请返回 HTTP 409。
- 运行时：2026-09-11 最终重建后，17 个 D5 容器均运行于 `erp-d5-20260910` 隔离网络/卷，网关和前端均返回 HTTP 200。
- 浏览器冒烟：P1 左侧无质检菜单且账户子菜单正常；Q1 工作台显示待质检任务，“前往质检”准确打开对应任务；质检页仅显示“待质检 / 已完成”两个标签，浏览器控制台无错误。

## 人工验收结论

- [x] 通过
- [ ] 需修改
- 验收日期：2026-09-11
- 反馈：D5 已完成人工验收。质检结果通知采购、采购端查看质检资料及部分合格先发流程暂列待确认项；不得因此阻塞本次 D5 收尾。

## 后续待确认项

- 质检完成后以通知、待办或其他方式告知申请采购员的具体口径。
- 采购是否需要在采购订单中查看质检结论、复检历史与附件，以及可见资料范围。
- 部分合格是否允许先发、由谁决定、如何记录放行数量以及何时同步物流。
- 五公式输入口径和物流费用分摊继续保留为跨阶段待确认项。
