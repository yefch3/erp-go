# D2 合同保存 500 修复

## 审批人打开合同误报产品目录 403

- B1 截图报错请求为 /api/products?page_size=200，并非审批操作接口。合同页面 mounted 无条件读取整个产品目录，B1 无 product:product:read 权限，触发无权限提示和未处理 Promise。
- 初始化及产品搜索均按 product:product:read 权限请求；合同已有产品快照正常显示，不为审批人扩大产品库权限。
- B1 登录后只读核对合同 249 和待办成功，审批任务 32 仍为 PENDING。未替用户同意或退回合同。
- 仅前端局部修复，D2 开发服务热更新；定价规则未变，未合并分支。

## 后续修复：手工产品显示和 400 请求错误

- 上轮将手工产品 ID 0 改为空值用于显示，未在提交时转换，导致 protojson 拒绝空字符串 int64；这属于上轮引入的前端回归。
- 草稿产品栏改为单一自动补全输入框：现有名称直接显示，可输入名称或选择已有产品；选中产品同步关联 ID 和单位，输入新名称解除旧关联。
- 保存草稿及合同变更提交统一将空产品 ID 序列化为字符串 0，保留名称、规格、单位、数量、单价。大整数 ID 不转 JS Number。
- 两项前端序列化回归及类型检查通过。
- 隔离环境新建询盘 26，经提交、客户报价保存和确认生成测试合同 252（CT-202609-0011）；HTTP PUT 保存并 GET 读回成功。
- 浏览器实测合同 252：产品栏只显示一个输入框；更名为“D2-网页保存回归冷轧卷”后点击保存成功，API 读回确认持久化。用户合同 249 未修改。
- 测试单明确命名为 D2 测试，保留为验收证据，未提交合同审批、未开始执行。前端开发服务热更新即可生效，无后端改动。未合并或推送。

- 用户验收合同 249 编辑保存时，UpdateContract 日志报 SQLSTATE 22P02：空字符串不能写入 numeric。
- 根因：普通合同重新生成的明细没有历史期初执行数量，writeContractItems 却把三个空字符串直接传给 AddExistingContractItem。
- 修复：写入边界将空的期初采购、到货、发货数量转换为 0；非空历史值原样保留。无需迁移或修改已有数据库数据。
- 编辑表单不再显示手工产品内部 ID 0；名称、规格、单位保留。接口失败后保留表单供重试，避免抛出未处理 Promise。
- 前端类型检查通过。新增真实数据库回归：普通草稿替换为手工产品后保存成功，金额 37.50，三个期初数量均为零；历史合同导入测试也通过。
- 测试仅连接 erp-d2-20260907 容器内部数据库，使用独立临时租户并清理。宿主机测试端口不可用，未更改数据库端口配置。用户合同 249 未由测试保存或修改。
- export 容器已重建并以 --no-deps 更新启动。未合并或推送分支，定价规则继续暂缓。

人工复验：重新打开合同编辑表单，修改交货日期或合同条款并保存；关闭后再打开核对修改已保存，产品名称、规格、数量及单价保留。不要为验证保存故障而提交审批或开始执行。
