# ERP V1 发布准备（尚未批准生产发布）

发布方向：`codex/erp-v1-rebuild-20260906` → `main`。复用草稿 PR #383，不另建重复发布 PR。不得自动合并 main 或触发 deploy。

## 本次补齐

- 从 main `dc35799c` 纳入星标优先、未读优先、双开优先级、升降序及滚动分页去重；保留 D1–D7 和已验收补齐规则。
- 相对 main 没有整份文件删除，但报价、质检、运输、财务及待办的现有实现被重建流程替换，不能把发布描述为纯新增。

## 业务与历史数据

- 采购仅提交工厂报价；销售按贸易条件选择公式、确认本次汇率并保留最终单价。客户确认后锁定报价快照。
- 一份合同一次运输；不开放部分合格先发。质检异常交采购处理，采购从采购单查看只读结论和附件。
- 运输节点、实际/预计时间、变更记录和协同提醒采用已验收流程。
- 报销使用已验收的部门确认、财务审批及出账流程；汇率参考不自动改写报价或财务数据。
- Git 合并不会复制本地验收账号或单据到生产。
- 迁移会调整审批定义、角色权限、状态及时间数据。尤其检查旧合同审批定义停用、质检权限收紧、采购收货状态和船期本地时间修正；生产存量数据未核验前不得声明无数据影响。
- IAM 两条历史的 59 编号含义不同，以 71/72 幂等补齐；之前已验证 main 59 与 rebuild 70 两种升级路径。生产仍须核对用户名大小写冲突及实际迁移版本。

## OnlyOffice 本次暂不新增启用

本地验收运行配置未配置 OnlyOffice，本次不将未经真实文档服务验证的在线预览纳入已验收范围。保留 Excel 直接预览、文件下载和既有 PDF 转换路径。

生产 compose 的 `DOCS_URL` 默认为空；该值为空时在线 Office 功能关闭。`DOCS_JWT_SECRET` 仍是生产 compose 必填项，即使功能关闭也不能删除这一要求。无法访问生产前，不能断言当前生产已启用或已关闭；不得擅自覆盖生产已有配置。真实服务与原件下载验证作为后续单独启用条件。

## 备份与发布闸门

- 本地验收数据库：全库备份为 3,803,214 字节，保存于忽略目录；SHA256 `00138EB58360BB4DC5D58A44B05A4999AFE61A2B4039220B6D03DDEBF15A42E0`。已在无网络的独立 PostgreSQL 容器完成恢复，IAM 版本为 72。绝不提交数据库内容到 Git。
- 生产：用户确认 AWS 账号为 `564623062079`、数据库为 `erp-pg`。当前本机只有 default profile，其身份属于另一个账号 `262588254055`，不能用于生产备份。仓库文档指向 `erp` profile/us-west-2，但该 profile 当前不存在。需要正确生产身份后才能创建并确认生产快照。
- 本地备份不能代替生产备份。生产备份、实际 schema/存量数据核对和恢复方案仍是发布阻断项。
- 正式发布前重新核对 main 头提交、最新完整 CI、数据库可恢复快照及上一版本镜像。
- 容器回退不等于数据库回退；不要对生产直接执行迁移 down。数据库恢复需单独计划并处理备份后新增数据。
- main 更新会触发 build，成功后自动部署。只有用户明确批准正式发布才允许合并。

## 验证记录

- 前端类型检查、生产构建通过；62 个测试文件、458 项测试全部通过（含星标/未读排序）。
- 13 个 Go 模块数据库集成回归进行中。销售到期日测试一度因数据库 ping 超时失败，正在环境稳定后复测，不将超时结果直接标为通过。
- 生产备份脚本 `production-backup.ps1` 已做语法验证，执行前强制核对生产账号；未获取正确生产登录前不执行快照。
- 最终状态以发布 PR 的 Checks 和完成记录为准，尚不标记可发布。

星标/未读置顶当前已进入本地发布准备分支，尚未进入远端 rebuild。中间分支推送及其指向 rebuild 的 PR 被自动审批阻止，已向用户单独请求授权；不能把 PR #383 的当前代码误报为已含该补齐。
