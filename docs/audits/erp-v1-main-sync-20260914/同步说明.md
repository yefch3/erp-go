# main 同步到 ERP V1 重建分支

起点：rebuild `46d7919b`；来源：main `4cd0f3f4`。
实施分支：`codex/erp-v1-main-sync-20260914`，独立 worktree。
本次仅提交同步分支，未经人工确认不更新 rebuild 或 main。

## 纳入内容

- 邮件实时刷新、未读筛选、滚动加载、拖动三栏宽度、嵌套文件夹修复。
- 独立邮件窗口、删除与更多操作、批量下载，以及主窗口刷新。
- 表格直接预览、日期格式与预览上限；OnlyOffice 在线附件预览及 S3 获取修复。
- 管理员配置登录名与密码，员工可用登录名登录。
- 前端资源按需加载；CI 缓存及按变更范围检查；旧镜像清理容错。

## 合并处理

- 导航保留 rebuild 已验收的折叠侧栏、移动端导航、角色菜单及运输/财务提醒。main 的另一套折叠状态不重复引入。
- Excel 同时保留 rebuild 的 OOXML 命名空间支持与 main 的日期格式、读取上限。销售询盘导入改用新的上限参数对象。
- 采购旧测试保留动态有效期，避免固定日期过期。
- IAM 59 在两边代表不同迁移：保留 rebuild 的原 59；main 的登录名索引移到 71，并以 IF NOT EXISTS 兼容已应用 main 的数据库。72 幂等补齐 D1 老板询盘权限，兼容从 main 升级时 59 已被记录的情况。

## 发布边界

当前工作流在 main 推送后构建镜像，build 成功触发 production 部署。最终 rebuild → main 合并应按正式发布安排。本同步分支不触发生产发布。

## 验证状态

- 前端 `npm ci`、`vue-tsc --noEmit`、生产构建通过；62 个测试文件、450 项测试通过。
- 所有服务在独立 PostgreSQL 中从空库迁移通过。
- IAM 从 main 59、rebuild 70 两种历史版本分别升级到 72 通过；登录名唯一索引存在，老板询盘权限正确补齐。
- 只读检查当前 rebuild 验收库：按不区分大小写统计，重复登录名组数为 0。
- 本地 13 个 Go 模块 `go test ./... -count=1` 全部通过，使用独立 PostgreSQL 25745、Redis 26683 并显式配置所有数据库集成测试与迁移测试 DSN；没有使用当前验收库执行测试。覆盖 D1–D7、报价及物流选择、质检采购协同、费用分摊、运输提醒、报销付款、角色权限和邮件功能的既有自动测试。
- GitHub 草稿 PR：https://github.com/yefch3/erp-go/pull/446 。同步提交 `c592d7ba` 的 [完整 CI](https://github.com/yefch3/erp-go/actions/runs/34799965125) 已通过：接口兼容性、proto/sqlc 生成一致性、迁移及权限种子守卫、前端全量检查、所有 Go 模块数据库集成测试和 lint。
- 本次没有切换当前前后端运行版本；原验收入口仍运行长期分支。真实邮件服务、OnlyOffice 及浏览器人工复验尚未完成。

## 人工复验重点

1. 原邮箱登录及新增登录名登录、角色菜单和运输/财务提醒。
2. 邮件文件夹、滚动列表、独立窗口删除/刷新、附件与表格预览。
3. 销售询盘文件导入，客户物流选择、公式计算、PDF 与成交锁定。
4. OnlyOffice 需要独立配置的文档服务和可访问附件地址，单元测试通过不等于已验证真实文档服务。
