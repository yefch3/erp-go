# D4 实施与验收记录

## 基线与隔离

- 起点：`f0b54dbb2f7b2457392f5b9e0b2993c067bfb401`
- 分支：`codex/erp-v1-d4-20260909`
- 独立 worktree：`C:\Users\16789\Documents\erp-go\.worktrees\erp-v1-d4-20260909`
- Docker Compose 项目：`erp-d4-20260909`
- 正式需求 SHA-256：`FE127EAAA0511434E5900FF19D403EC05CC01215237E6F6BA95346CEDA248E47`

环境脚本验证 17 个容器全部属于 D4 专用网络和卷，数据库连接只指向 D4 PostgreSQL，网关健康检查为 200。

## 已实现范围

- 财务确认执行条件后才生成采购与物流待重新询价任务；重复确认不会重复生成或覆盖决定。
- 售前工厂、货代和船公司只作参考，不锁定正式对象，也不回写客户报价或外销合同。
- 采购实单询价允许为每项产品保留多家工厂的新报价，并从中选择一条最终报价；生成采购订单草稿时按最终工厂、币种、交货日期和付款方式分组。
- 采购先在草稿中复核后提交一次上级审批；同意后进入“采购合同”，采购上传双方签署合同后仍留在该页并显示“待财务审核”，财务审核并批准付款后才进入“已下单”；退回后退出采购订单列表并释放产品回到实单询价重新报价和选厂。
- 物流在“实单物流询价”中保留多家货代或同一货代的多套报价，并在该页选择最终方案提交到物流订单草稿；物流订单草稿只展示已选方案，复核后再提交经理审批。物流订单统一展示草稿、审批中、物流合同和已委托。实际承运船公司可留待宣船时补充，货代合同号在上传签署合同时填写。
- 物流询价详情保存并展示合同产品快照，包括产品、规格、数量、单位和备注；不携带销售价格，也不在本阶段执行产品级物流费用分摊。
- 物流合同上传后同页显示待财务审核；物流以最终货代为合同和收款对象，财务在统一出账审核通过后订单直接进入已委托。
- 我的待办接入物流重新询价上级确认，并准确跳转到物流订单“审批中”的对应任务。
- D4 生成的付款申请禁止超付；旧手工付款行为保持 D1–D3 已验收边界。

## 自动验证结果

- Go 全模块测试：通过。
- 前端类型检查：通过。
- 前端测试：55 个测试文件、345 项测试全部通过。
- 前端生产构建：通过。
- Proto lint：通过。
- Proto 相对 `f0b54dbb` 的 breaking check：通过。
- 迁移安全、租户字段和网关重复路由检查：通过。
- D3 放行边界回归：通过。
- D4 端到端：通过。自动合同 `CT-202609-0011`，3 项产品按 2 家最终工厂拆成 2 张订单；采购付款申请 2 笔。物流任务 12 依次完成保存草稿、提交审批、上级通过、上传签署合同和财务审核，最终状态为 `PAYMENT_REQUESTED`（界面显示“已委托”）；最终货代为“D4 验收货代”，实际承运船公司保持为空，货代合同号为 `D4-LC-SMOKE-12`，物流未付金额为 `USD 1280.00`；未创建 D5 业务。
- 物流多候选方案冒烟：通过。同一临时任务保存 2 份候选方案，选择第二份“中转方案 / USD 1350.00”提交后，审批主记录准确采用该方案；临时任务、候选方案和审批记录已清理。

结构化证据位于 `evidence/d4-api-result.json`、`evidence/d4-flow-result.json`、`evidence/accounts.json` 和 `.d4-environment/runtime-verification.json`。

## 保留边界

- 用户已完成人工验收，并授权最终回归通过后合并到 `codex/erp-v1-rebuild-20260906`；GitHub Checks 和合并结果以最终 PR 记录为准。禁止合并到 `main`。
- 未进入 D5 的备货、质检、订舱和运输执行。
- 五公式输入口径和物流费用分摊仍是待确认项，本阶段没有修改。

## 最终验收收尾

- 已确认物流上级 LM1 的权限、L1 的直属上级关系和待办定位；采购审批由 B1 处理。
- 最终端到端记录：采购订单 27、28；物流任务 12 保留 2 个候选方案，选定方案 6 后才生成草稿并提交审批。
- 物流审批初始化迁移按各租户建立定义，避免仅第一家公司可用。
- 五公式输入口径与物流费用分摊仍待确认；未开始 D5。

- 最终真实数据库集成测试通过：approval、export、iam、inventory、masterdata、procurement、product、shipping、mail 九个模块启用专用 TEST_DSN；临时 PostgreSQL 与验收数据隔离。
- 前后端冒烟通过：4 个前端入口、代理健康接口返回 200；P1、L1、LM1、B1、F1、S1 登录及对应业务接口通过，物流最终状态保持已委托。
- 本轮全 Go 模块测试、前端类型检查、55 文件 / 345 项前端测试、生产构建及 Proto 兼容性检查通过。
