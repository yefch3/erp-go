# 报价流程简化：2026-09-13

- 采购恢复单一 price 输入，FOB/factoryPrice 字段仅用于读取旧数据兼容；采购、物流提交不再要求贸易条款。
- 物流新提交必须统一币种和计价单位，列表展示一个报价合计；计算所需费用构成在详情。
- 贸易公式移至客户报价头部，按 FOB/CFR 筛选；删除统一核价按钮和抽屉。
- 工厂价只读，客户数量、单位与最终单价可编辑。吨/件换算使用每销售单位吨数。
- 所选物流报价ID随客户报价保存；后端读取原始物流费用重新核对计算，不信任客户端修改的费用。
- CFR候选方案可以正常加入，选定后核价且总额不重复加运费；分摊使用所选物流来源，财务付款流程不拆单。
- 保留旧混合币种和多个工厂测试案例92；新版人工验收使用案例93。
- 前端类型检查、5项报价单元测试、export/procurement包测试及5公式实际API验证已完成；页面已以S1验证公式3自动得出130.00。
- 人工验收已于 2026-09-13 完成；用户确认当前逻辑无问题，并授权执行最终测试、提交、推送与长期分支合并。

- 客户物流选择和最终成交拆成两个入口；FOB 使用内部国内费用来源，不要求客户选择海运。
- 新增 JSON 核价快照（无数据库表结构变更），跟踪公式、汇率、单位、数量、物流选择及选定上游报价。旧价失效时阻止保存、PDF 和成交；销售手工议价不使快照失效。
- 原有 93 号人工验收数据未重置，旧核价需由验收人重新选择并计算。

### 本次验证结果

- export 应用单元测试通过；新增核价快照、议价保留、输入变更失效和 FOB 无客户海运选择测试通过。
- 前端 typecheck 和 customerOffer 的 5 项测试通过。
- 实际 API：五公式单价依次为 115.00、114.29、130.00、135.00、125.00；旧价保存被拒绝；93 号人工验收数据保持原样。
- 全新自动回归单 96（SC-20260914-000089）：公式核价后将第一项议价为 118.00，保存/重读均保留，合计 348.00；PDF 成功，生成测试合同 240 后锁定，后续修改被拒绝。
- 浏览器验证 93 号旧价提示、选方案弹窗、重新核价及独立成交预览；未保存或确认该人工验收单。
- 17 个隔离验收容器运行正常，gateway 返回 200；已重建 export 服务，前端由 Vite 更新。
- 未提交、推送或合并长期分支。

自动回归中曾出现旧响应，原因是测试脚本 WebSession 复用了上一次请求的防重标识；已修正为每次请求独立标识，并以全新 96 号单验证通过。

### 公式切换后最终操作反馈

- 用户反馈公式 2、汇率 6.72 下确认成交或 PDF 的 400；相同数据在当前服务未复现，不能仅凭截图认定服务端错误码。
- 报价命令错误保留在页内，服务端报告旧核价时同步界面失效状态；重新计算失败不覆盖原计算数据。
- 旧核价时禁用导出，与成交保护一致，底部明确先重新计算。
- 专用回归单 95 验证公式 1 → 2、计算、页面保存、重读无失效；公式 2 单价 119.66，PDF 成功，测试合同 241 生成成功。93 号人工验收单未保存修改。

### 最终回归

- 前端类型检查、生产构建、58 个测试文件和 389 项测试全部通过。
- 全部 Go 模块单元测试与隔离 PostgreSQL 集成测试通过；所有 Go 服务模块 lint 通过。
- Proto、sqlc、迁移安全、租户隔离、权限种子、邮件沙箱、重复路由及生产 Compose 环境规则检查通过。
- 真实 API 冒烟覆盖采购质检通知与待办、附件与权限、五公式、报价锁定、客户物流选择、分摊精度及一次运输约束，结果均符合验收口径。
