# D3 人工验收步骤

## 测试入口与账号

- 前端：`http://127.0.0.1:25274`
- 网关：`http://127.0.0.1:28181`
- 所有业务测试账号密码：`D3-Acceptance-2026!`
- 销售：`s1@d3.example.test`
- 上级审批：`b1@d3.example.test`
- 财务：`f1@d3.example.test`
- 采购：`p1@d3.example.test`
- 物流：`l1@d3.example.test`

验收客户为“D3 验收客户”。请使用合同 `CT-202609-0002`，它有 3 个产品、金额 USD 2167.50，目前已经完成审批、签署和销售开始执行，正停在财务确认前。另一个合同 `CT-202609-0001` 已由自动验收走完财务放行，可用于对照。

## 建议验收顺序

1. 使用财务账号进入财务入账/应收清单，找到 `CT-202609-0002`。确认它显示“等待财务确认”，并有“确认执行条件”按钮。
2. 打开确认窗口，确认可选择以下四种条件：预付款已收到、信用证已收到、无需预付款、特殊情况允许执行。选择任意一项，可填写备注，然后提交。
3. 提交后确认该合同显示“可以执行”和刚选择的条件；刷新页面后状态应保持不变。
4. 使用销售账号打开 `CT-202609-0002`，确认合同只显示“财务执行条件：可以执行”，不显示财务经办人、确认时间和内部备注。
5. 使用采购账号进入代采购任务，找到该合同的批次。应有 3 项产品，状态均为“待重新询价”。售前工厂/报价只能作为参考，界面不得提供创建采购单的按钮。
6. 使用物流账号进入物流页面，找到该合同的“合同执行待处理”。应显示“待重新询价”，不得锁定货代，也不得提供建立正式船期的操作。
7. 返回财务页面再次刷新。确认采购需求和物流任务没有重复增加。

## 验收判定

以上步骤全部符合即可确认 D3 通过。发现问题时请记录账号、合同号、操作步骤和页面现象。人工确认前，D3 不会合并到长期分支或 `main`；也不会开始 D4。
