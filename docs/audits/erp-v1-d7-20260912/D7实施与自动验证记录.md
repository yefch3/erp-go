# D7 实施与自动验证记录

更新日期：2026-09-13

## 实施结果

- 在既有“出账”页面内增加出差报销，没有建立重复模块。
- 报销按部门负责人、财务负责人两级审批；禁止自审，财务负责人本人申请回退至老板审批。
- 凭证是提交前置条件，关键资料或主要凭证变化使已通过审批失效；已付款资料锁定。
- 财务负责人可查看全公司报销、登记付款及带原因冲销；普通员工只看本人或本人参与审批的记录。
- 我的待办接入 `TRAVEL_REIMBURSEMENT`，审批推进事件同步报销状态。
- 独立银行流水页面与写路由下线；历史数据和只读接口仅作兼容保留。实际收款、付款分别归入“入账”的“收款记录”和“出账”的“付款记录”。
- 出差报销页面取消状态筛选和处理记录展示，保留后台审计；提供“保存并提交审批”，审批后自动刷新最终状态。
- 汇率改为全员可查看的独立参考页，每小时同步、手工刷新、关注币种、30 日趋势、来源/时间和缓存降级状态齐全。

## 自动验证

- 前端：`vue-tsc --noEmit` 通过；Vite 生产构建通过；Vitest 58 个文件、389 项测试通过。
- Go 全模块：`pkg`、`gen`、approval、export、fx、gateway、iam、inventory、mail、masterdata、procurement、product、shipping 全部 `go test ./...` 通过。
- 真实数据库：采购报销生命周期测试通过；IAM 迁移角色与新租户预置角色对表测试通过。
- 迁移：approval 9、procurement 67、fx 4、iam 69 已在测试库应用；approval、fx、iam 的 D7 迁移均完成 `down → up`；采购全迁移此前完成 `up → down → up`。
- 静态守卫：tenant_id、IAM 种子、租户默认数据、迁移安全、重复路由、Compose 必填变量全部通过。
- 协议：Buf lint 通过；生成代码可编译。
- API 冒烟：报销创建、无凭证拦截、附件上传、两级审批、自审 403、无关员工不可见、付款、历史、旧银行写入口 405、汇率缓存降级和实时恢复全部通过。证据见 `api-smoke-evidence.json`。
- 运行环境：D7 全部后端容器启动，网关 `/api/healthz` 返回 200；前端 25375 返回 200，L1 经前端代理读取实时 CNY 汇率成功。

## 人工验收结果

- 用户于 2026-09-13 确认 D7 人工验收通过。
- 验收期间确认部门负责人、财务负责人两级审批、付款登记、入账/出账历史记录、权限边界与独立汇率参考页符合当前业务口径。
- 验收反馈涉及的报销页面精简、异步审批状态刷新、Element Plus 告警、历史收付款入口与操作文案均已修正并纳入最终自动回归。

## 最终提交前状态

- 分支：`codex/erp-v1-d7-20260912`
- 起点：`e260588fdcd84c22608b703370b7c330c715c218`
- 人工验收通过；待提交、推送并通过 GitHub Checks 后，仅合并至 `codex/erp-v1-rebuild-20260906`，不得合并至 `main`。
- D5 暂缓事项、五公式输入口径、物流费用分摊和多币种混合汇总仍保持待确认。
