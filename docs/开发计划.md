# 开发计划：阻塞业务的与提升效率的

> 建立于 2026-08-19。来源是三份需求清单（采购、基础数据、船期）的一次交叉核验。
>
> **为什么单独立这一份**：三份清单加起来有 240 条未完成项，但里面混着测试项、
> 上线前置（运维类）、明确标注“不要开发”的候选需求，以及**做完了没更新状态**的。
> 直接照着清单排期会既做错事、又漏做事。这一份只留经过代码核验的业务缺口，
> 按“不做会怎样”排序，供逐条讨论。
>
> **核验方法**：不看清单里的 ✅/⬜ 标记，改为查三层——数据库迁移、服务端实现、
> 前端页面。三层缺任何一层都不算做完。每条都附证据位置。
>
> 状态：⬜ 未开始 · 🔶 部分完成 · ✅ 完成（完成时把结论回写到对应需求清单，
> 并在本文件划掉该条）。

---

## 一、阻塞业务的

不做，用户就得退回 Excel 或手工——也就是这套系统在这一段没有替代掉任何东西。

### A1 ✅ 采购订单与采购需求的数据范围（2026-08-22 全部完成）

**订单半边（2026-08-21）**：`order_scope.go`，模块 `procurement_order`，
按 `buyer_id` 围栏：列表 SQL 过滤 + 全部按单寻址的 RPC 在 grpcin 加闸，
越权一律答「不存在」。供应商发票同模块围栏（按录入人）。角色页
「采购订单可见范围」旋钮；iam 00037 种子。

**需求半边（2026-08-22，属主决策拍板后收尾）**：业务定的归属规则——
**合同拆出的需求归合同负责人，手工需求归创建人；采购单归实际执行的
采购员，订单明细上保留合同负责人**（换人下单不丢谈单人）。实现：
`ContractEffective` 事件带上 `sales_employee_id/sales_employee`，需求生而继承；
模块 `procurement_requirement` 围栏（列表过滤 + 详情/关闭/重开/覆盖订单/
模板导出按单加闸）；`owner_id=0` 的历史行只有「全部」范围可见
（fail-closed）；角色页新增「采购需求可见范围」旋钮，iam 00041 种子。

> **上线两步（一次性）**：① 跑 `scripts/backfill-requirement-owners.sh`
> 从 export 合同表补齐存量属主；② 管理员在角色页把**采购类角色**的
> 需求范围开到「全部」——待采购池子是工作队列，按人切会把活干断；
> 销售角色保持「仅本人」，只看自己客户的采购动向。

### A2 ✅ 工厂报价横向比较（2026-08-22 核验后收尾完成）

**核验修正**：原文写的六条缺口里有三条是**过时信息**——P5/P6 询价工作流
（PR #108 之后的几期）早已建成：版本化报价（`version_no` 自增，比较页只用
最新版）、按产品分组的横向比较（价格/交期/MOQ/付款条件/有效期/最低价标记，
`ListSupplierQuoteComparison`）、成本方案逐行选不同工厂报价（含完整性校验）。
又一个「做完了没更新状态」的案例，和 B2 同病。

**真实缺口三条，2026-08-22 一并补完**：
- 导入错误不带行号 → 逐行报 Excel 行号与原因，一次报全
  （`SC_QUOTE_ROWS_INVALID`）
- 跨币种没法比价（最低价只在同币种内比）→ 比较行带账本币换算价
  （读取时现算、不落库；汇率不可得则该列留空，前端退回同币种排序）
- 中标无原因、不进历史 → 确认成本方案必填原因
  （`SC_COST_REASON_REQUIRED`），操作人/时间/原因写入
  `cost_scenarios.confirm_reason` 与案件变更记录（COST/CONFIRMED）

### A3 ✅ 质检记录（2026-08-22 完成）

质检表 `purchase_inspections` 挂收货单：结果（合格/不合格）、抽检与
不合格数量、附件、处置（退货/扣款/让步接收/返工 + 备注）。合格生而
已处置；不合格开着等处置决定。收货—质检—入库的关系定为：收货即入
码头库（现状不变），质检事后登记，不合格不改库存——处置是人的决定，
记录供扣款与纠纷取证。

**结案闭环**：结案做成 `closed_at` 列而非新状态（状态机 RECEIVED 封顶，
对账/工作台按状态过滤处一概不用改）。三道闸：全部收满、无未解决到货
异常、无未解决质检；拒绝时带两类未决数量。权限单独一码
`procurement:order:close`（iam 00040，种子只给 SUPER_ADMIN）。
前端：执行跟踪新增质检页签，列表 RECEIVED 单主动作变「结案」，
结案后亮绿色标签。

### A4 ✅ 供应商应付对账（2026-08-22 全线上线，状态回写补记）

P1–P6 加银行流水 CSV 已全部建成并上线：供应商发票（三单匹配、
EXCEPTION 亮警）、付款与核销（append-only、冲销为负行、状态派生）、
预付与押金、对账视图（五组数字并排 + 逐笔流水滚动余额）、多币种汇率
快照与汇兑损益、银行对账单导入与付款匹配。本条早已完成但状态一直
没回写——又一个「做完了没更新状态」，2026-08-22 补记。
>
> 详细方案见 [供应商应付对账实现方案.md](./供应商应付对账实现方案.md)。

### A5 ✅ 货没到齐也能关单，差额回到待采购（2026-08-23 完成）

**场景**：订了 100 吨，工厂只能给 80 吨，也确实只发了 80 吨。在此之前
这单永远关不掉——关单要求「全部收满」，而那 20 吨永远不会来。货收完了、
钱付了、事情办完了，系统却一直在等，财务和采购只能在系统外记一笔。

**业务定的做法**：关掉这单的时候，把没到的 20 吨放回待采购清单，人点一下
确认，然后另找一家工厂开新单。

**实现**：关单时如果货没到齐，必须回答两件事——剩下的另找工厂买还是不要
了、以及为什么。选「另找工厂」就把差额退回待采购（用的是取消订单时那套
现成的 `ReleaseRequirementOrdered`），需求状态回到「部分已订」，新单能照常
从池子里领走它。

**订单明细的数量一个字不改**：「订了 100」是事实，「认 80 就算完」是判断
——同 A4「付款是事实、核销是判断」、A3「收货是事实、结不结案是判断」。

两道闸仍然在：悬着的到货异常和质检照样拦着不让关。

> 一个需求本来就支持多张采购单（`open_qty = 要的 − 已下单的`），所以差额
> 一旦回到池子，「80 吨找甲厂、20 吨找乙厂」就是现成的路。缺的一直只是
> 「把没到的部分放回去」这一步。

#### A5 续：一开始就知道只供 80 吨（2026-08-23 完成）

上面写的是「订了 100，到货只有 80」。业务真正常遇到的是另一件事：**询价
比价的时候就知道这家只能供 80 吨**。这时该做的不是等货不齐再关单，而是
直接按 80 吨下单，剩下 20 吨另找一家或过些天向同一家补。

把四条路挨个在系统里跑了一遍（`TestA5Probe`，一次性探针），结果：

| 操作 | 当时 | 现在 |
|---|---|---|
| 一开始就只订 80 | 能 | 能 |
| 剩下 20 吨另找一家（合同来源的需求） | 能 | 能 |
| 作废还没到货的采购单，重新采购 | 能 | 能 |
| 剩下 20 吨另找一家（**客户报价来源**） | 挡住 | **写原因放行** |
| 剩下 20 吨还找同一家补购（**客户报价来源**） | 挡住 | **写原因放行** |
| 已经到了一部分的单再作废 | 挡住 | 仍然挡住（见下） |

**为什么原来挡住**：这条线上供应商和数量是询价比价定死、报给客户、再签
合同的，所以系统要求采购单的供应商必须等于成本方案里那家（换一家，报价
时算的利润就变了），另有一条唯一索引拦重复点击下单，顺带把正当的补购也
拦了。顾虑对，做法太硬。

**改法**：照这里现成的规矩办——偏离已确认方案，**写明原因就放行，原因随
单存档**（`source_change_reason`，和「改了确认报价的单价或币种要写原因」
同一个字段）。前端默认仍然锁着供应商下拉框（这是常态），要换得先点一下
「改向其他供应商」，是一次明确动作而不是随手改。

**防重复点击没有丢**：真正的把关本来就在别处——下单事务开头把需求整行锁
住，且不允许超过「还能下多少」。唯一索引降级成普通索引，判断挪到应用层
并分成两种：**还停在草稿的**仍然当重复处理（前端认这个错误码，会把上一张
调出来接着办），**已经确认下单的**才是补购，写原因放行。

**顺带补一个洞**：「待采购并审批」页原来只列「一点没买」的需求，下了第一
张单整批就从眼前消失了——剩下的 20 吨得靠人记着。改成把「部分已订」的一
并列出，买齐了才消失。

**「已经到货了还能不能作废」没有放开**：50 吨已经在码头库里，作废会让这
批货不属于任何订单、开不了票也付不了款。系统原来提示「请走退货流程」，
但退货流程其实只有一条记异常的记录、不会退数量——这句提示是空头支票。
这种情况应该走关单那条路：认下已到的 50 吨、照常开票付款，剩下的放回待
采购另找工厂。**待业务确认**是否确实有「货到了但整单要推翻」的场景。

---

## 二、明显提升效率的

有替代做法，但很痛。

### B1 ✅ 成本方案闭环（2026-08-22 核验后收尾完成）

**核验修正**：三条缺口里一条半是过时信息——「已生成客户报价」状态
（`CUSTOMER_QUOTE_CREATED`）早就存在；「拒绝后建新版」机械上也早就通
（`CreateCostScenario` 允许在该状态下再建方案，确认时自动 SUPERSEDE 旧版）。
真断的是**信息回路**，2026-08-22 补完：

- 报价单拒绝时记录客户原话（`quotations.respond_note`，选填——客户
  没说就留空），列表展示
- 案件页看得见被拒：最新报价单被拒时，成本方案区顶部亮横幅（带客户
  原话），引导基于反馈建新一版——采购员不用自己想起来去翻报价单页
- 成本构成分区展示：方案详情拆出产品成本 / 附加费用（含明细表）/
  落地成本 / 利润 / 客户报价，不再只有一个总数

### B2 ✅ 船期的数据范围与操作记录（核验后发现早已建成）

**2026-08-21 核验修正**：原文「与 A1 同一个病，船期这边也没治」是**过时
信息**。实际上船期这边机制完整且在用：`services/shipping/internal/app/access.go`
（模块 `shipping`，负责人 + 客户负责人双维度），`authorizeSchedule` 有
14 处调用覆盖详情/编辑/取消/路线/单证/提醒，列表与统计走 `visibleTo`
过滤，范围种子在 iam 迁移 00016；变更记录 `addChange` 有 19 处调用。
本条不再是待办。

### B3 ✅ 船公司/货代主数据（2026-08-22 核验后收尾完成）

**核验修正**：这条的过时程度是三兄弟里最重的——「手工文字」早已不是现状。
主数据就是**供应商**（B4 供应商管理把业务类型做成了多选：船公司/承运人、
国际货代、报关、仓储……同一家可兼多角色 ✓，编码/名称/国家/联系人/状态/
变更历史全在 ✓）；船期表单早已是下拉选择并存 `carrier_id` + 名称快照
（停用后历史保留 ✓，迁移 00004 的注释写明了快照设计）；网关也早在校验
「启用的供应商」。

**真缺三针，2026-08-22 缝上**：下拉不过滤（钢厂和停用的都混在候选里）
→ 只列启用的 CARRIER/FORWARDER 并集；网关不透传 `business_type` → 透传；
服务端不验角色（选一家钢厂当船公司也放行）→ 业务类型必须含
CARRIER/FORWARDER（`SHIPPING_CARRIER_ROLE`）。**按船公司统计延误率的
地基自此打通**（carrier_id 可聚合），报表本身另立条目再做。

### B4 ✅ 采购工作台收口（2026-08-22 完成）

五条缺口全部补齐：订单指标拆成「待审批 / 待正式发单」两个行动数字
（待发单走 ListOrders 新增的 `unsent` 过滤，沿用列表自己的数据范围围栏）；
指标点进去落在筛好的列表上（订单页接收 `?status=`/`?unsent=1`）；
今日重点补上逾期 RFQ（新 RPC `ListOverdueFactoryRfqs`，按询价案件属主
围栏）、待审批订单、待确认成本方案三源；按紧急程度 + 截止日期排序
（逾期的最上）；重点卡头部显示分来源数量。

### B5 ✅ 采购订单列表收口（2026-08-22 完成，尾巴同日缝上）

列表只留「详情 + 当前主动作」（草稿→提交、批完→发单、发完→收货、
收完→结案，按状态推导），其余动作收进「更多」下拉；状态列增加发单
子标签（已发单/未发单/发送失败），审批状态与发单状态从此分开可见。

**两个尾巴已缝**：状态列在已发单之后再挂工厂回签子标签（待工厂确认/
工厂已确认/差异审批中/确认被驳回，取最新一条回签，列表 SQL 一个子查询
带出）；详情页顶部加「下一步」条，与列表主动作同一套推导，点了直接
落进对应弹窗——看完单子不用回列表找按钮。

---

## 二点五、AI 辅助（C 组）——后面慢慢加

> 2026-08-22 立项。已有先例：邮件 → 标准询盘 Excel 的模型提取
> （`services/mail/internal/adapter/openai/table_extractor.go`，提示词在
> 同文件 `extractionPrompt`）。这套系统适合加 AI 的原因是所有关键落库点
> 都已有校验器把门（报价模板校验、三单核对、逐行报错、金额平衡），所以
> 统一原则是：**AI 出草稿，人确认，守卫兜底**——模型错了会被现有的闸
> 拦住，不污染账本。按痛点 × 可行性排序：

| # | 内容 | 下游守卫 | 状态 |
|---|---|---|---|
| C1 | **工厂报价解析**：任意格式的工厂回复（邮件正文/乱 Excel/PDF）→ 标准报价预填，人确认入库。现在只认锁定模板，模板外全靠手敲 | 报价完整性校验、比价页人审 | ⬜ |
| C2 | **发票扫描件 OCR 预填**：扫描件（已在 S3）→ 读出发票号/金额/明细预填录入表单 | 三单核对会把抽取错误亮成 EXCEPTION | ⬜ |
| C3 | **询盘产品匹配建议**：复核页 NO_MATCH 的行，从产品库给前三候选 | 人点选，不自动匹配 | ⬜ |
| C4 | **邮件三件套**：进件分类（询盘/报价/物流/垃圾）、英西回复草稿、长线程摘要 | 草稿不自动发送 | ⬜ |
| C5 | **银行对账单兜底**：解析器认不出的格式转标准列；对方户名 ↔ 供应商模糊匹配增强建议 | bank_ref 去重、建议不代劳 | ⬜ |
| C6 | **智能路由**：让用户按活儿挑模型（快/准/便宜），见下方 C6 一节 | 现有的表格校验与人工复核照旧兜底 | ⬜ |

### C6 ⬜ 智能路由：让用户按活儿挑模型

**核验结论（2026-08-24）：地基已经有一半，另一半是个陷阱。**

已经有的：

- `mail_excel_jobs.model` —— **每次转换用了哪个模型早就落库了**（`00035_excel_jobs.sql`
  第 16 行就有这一列），不用新建字段
- 转换结果一路带着 `Model` 出来，worker 存的时候顺手就写了

缺的两样：

- **模型是一个进程一个**：`OPENAI_MODEL` 从环境变量读（`config.go:119`，默认
  `gpt-5.6-luna`），全公司、所有人、所有转换都是它。要换只能改配置重启
- **单价也只有一份**：`ModelPricing` 是一个结构体、一对数
  （`excel_usage.go:23`），`ExcelUsageByMonth` 按「月 + 人」分组，**不按模型分**

#### 先说那个陷阱

单价是一份，路由是多模型。**这两件事一起上线，钱就算错了**——用 Sol 跑的那次
会被按 Luna 的价算。而 5.6 家族最贵和最便宜差 **20 倍**（Sol $4/$20 对
Luna $0.20/$1.20），错一次就错一个数量级。

**所以顺序是死的：先做按模型分价，再放开选择。** 反过来做，用量页会给出一个
看着像账、其实是错的数字——**比没有数字更坏**，因为没人会去怀疑它。这和
「空 ≠ 0」是同一条：算不出来就该说算不出来，不能给个像模像样的错数。

#### 三步，顺序不能换

| 步 | 做什么 | 为什么卡在这个位置 |
|---|---|---|
| 一 | **单价按模型配**：`ModelPricing` 从一份变成一张表（模型 → 输入价/输出价）；`ExcelUsageByMonth` 的分组加上 `model`；用量页按模型分行 | 没有这一步，后面两步都在算错钱 |
| 二 | **可选清单**：管理员配「本租户允许哪几个模型」，各自的价和一句人话说明。**不给自由填**——填错一个型号名就是一次失败的转换加一次白花的钱 | 先收口，再放开 |
| 三 | **转换时选**：生成 Excel 的入口给个下拉，默认走清单里的默认项 | 前两步做完这步很小 |

#### 限量必须同时上

Sol 比 Luna 贵 20 倍。**放开选择而不限量，等于把成本上限交给点鼠标的人。**
这件事和「限量第二期」（Redis 突发限流 + 租户月度配额）是同一件事，应该
一起做，不该分两次。

#### 做成公共件，不要只做邮件

C1（工厂报价解析）落地时会是第二个花模型钱的地方，C2 之后还有。选模型、
按模型计价、按模型限量这三件事对它们是一样的。**第一次就做成公共的**，
否则第二个落点会把这套逻辑再抄一遍，然后两份慢慢长歪。

#### 待业务确认

1. **真的需要选吗**——Luna 现在的提取效果够不够？够的话这件事的价值就只剩
   「难啃的附件上更好的模型」，是个窄场景，优先级应该往后放
2. **谁有权选**：所有人，还是只有主管？
3. **选贵的谁买单**：按人算额度，还是按租户算总额？

**工作量**：步一小 · 步二中 · 步三小 · **依赖**：步一必须先行；与限量第二期
同期 · **来源**：2026-08-24 查 OpenAI 实际单价时提出

---

## 二点六、流程补全（D 组）——对照标准外贸流程图立项

> 2026-08-22 拿一张标准外贸 ERP 流程图（报价单 → 合同 → 审批 →
> {信用证 / 出货计划 / 生产任务 / 采购订单} → 订舱 / 退税 / 收汇 /
> 入库 / 付款 → 单证 / 一览表 / 统计）逐节点对照。**主干完全吻合**，
> 且质检/结案/回签/对账/银行流水这些图上没画的我们已经有。缺四块，
> 按「钱 × 频率」排序：

| # | 内容 | 前置 | 状态 |
|---|---|---|---|
| D1 | **出口退税**：见下方流程与实现思路 | 备案信息与首次流程需业务确认 | ⬜ |
| D2 | **合同执行进程一览表**：见下方 D2 一节 | 无 | ✅ 2026-08-23 |
| D3 | **信用证（L/C）**：收证登记、条款要点（金额/效期/最迟装运日/交单期）、到期提醒、不符点记录、关联合同与出运单——本质是期限与单据的台账 | ~~待业务确认~~ **前提已成立**（见下） | ⬜ |
| D4 | **单证制作 + 统计分析**：成套单证（发票/箱单/提单草稿）生成；经营报表。前者当时明确缓做，后者等数据积累——别为空库建报表 | 前者等船期单证真用起来；后者最后做 | ⬜ |

> **D3 的前提在 2026-08-23 核实后已经成立。** 计划原写「待业务确认：客户
> 结算是否有 L/C，全 T/T 则永久搁置」——业务早已答过：「收到定金/预付款/
> **信用证**之后就去采购那边下单」（同 E1 的账期口径）。代码里也有痕迹：
> 付款方式的下拉框里「信用证」是现成选项，审批业务类型的中英西三份翻译里
> 都躺着「信用证改单」——但后端没有任何东西产生它，是个空标签。所以 D3
> 不是待确认，是**未开工**：缺的是收证登记、条款要点与到期提醒这套台账。

### D2 ✅ 合同执行进程一览表（2026-08-23 完成）

**为什么要它**：「这单到哪了」原来没有一个人答得上来。合同金额在出口合同
页，订没订货在采购页，货走没走在出运页，钱收没收在银行流水页——四个页面、
四个部门，谁都只看得见自己那一段。

**一行一合同，四段进程**：

| 段 | 口径 | 为什么这么定 |
|---|---|---|
| 采购 | **按项数**：共 N 项 / 订齐 M 项 / 到齐 K 项 | 一张合同上 100 吨钢卷和 50 件配件加不起来；折成金额又会把采购成本混进一张讲营收的表 |
| 出运 | **按金额**：已出运额 ÷ 合同额 | 和收款同一把尺，两个数直接可比——走了六成货、收了三成款，钱货脱节一眼看得出 |
| 船期 | **最新一班**的状态 + 班次数 | 问的是当下；历史班次点进船期页看 |
| 收款 | **按金额** + 逾期天数（复用 E1 的到期日） | 同上 |

**取数的形状**：出口那边三条腿（合同 / 已出运 / 已收款）在同一个库里，一条
SQL 出完，分页和数据范围都由它定；采购和船期在别的服务，按**这一页的合同**
批量问回来拼上——**一页两次调用，不是一行两次**。20 行也是两次。

**折算的三条讲究**（都在测试里钉住了）：按产品配对而不是按明细行（改版会
重写行 id，按行配对会让改版前发出去的货凭空消失）；单价取生效版本的**加权
均价**（同一产品偶尔落在两行上）；**超发不夹紧**——改版把量调小而货已经发
了，13000 > 合同的 10000 要露出来，不是抹平。

**围栏只设一道**：合同能不能看，由出口那道现成的围栏判；采购和船期两列
**不再单独设栏**。这是刻意的——同一行里有的列有数、有的列空着，读起来就是
「没采购」，而真相是「你没权限看」。**一个会给出错误答案的表格比没有这张
表格更糟。** 安全性靠另一头保证：这一行只给进度数字，不给供应商、不给采购
价；要看细节得点进去，那时各模块自己的围栏照常生效。

**一条腿断了不装作是零**：采购或船期取不回来时，那一列写「取不到」并打灰，
顶上横幅说明「不是『没有』，是『没问到』」。零在这张表上的意思是「还没开始
采购」，和「服务连不上」是完全不同的两件事，读的人会照着做出完全不同的决定。
顶部那个「货还没订齐」的合计也一并让位——浏览器实测时正是它先露的马脚：
停掉采购服务后数字从 2 跳到 4，那 4 是个吓人的假数。

---

### D1 出口退税：流程与实现思路

**业务给的流程（2026-08-23）**：

1. **退（免）税备案先做** —— 企业级的一次性动作，没备案后面全都白搭
2. **海关下报关单** —— 出口报关后拿到报关单，这是退税的起算凭证
3. **准备对应的进项发票** —— 买货时供应商开的增值税专用发票，退的就是这笔进项税
4. **其余单据备齐** —— 出口合同、运单、报关委托协议等
5. **第一次会比较麻烦** —— 首次申报的资料多、口径生，之后是重复劳动

**这句流程改变了实现方向**：退税模块的核心**不是算税**（税率查表、金额乘除，Excel 都能做），
而是**「一批出口的单据到底齐没齐」**。真正的痛点是：一票出口要凑齐报关单 + 进项发票 +
合同 + 运单 + 委托协议，缺一样就申报不了；笔数一多，人靠脑子记哪票缺哪张。所以这个模块
本质是**按出口批次归集单据的清单 + 缺件提醒**——和 A3 质检、A4 三单核对是同一种东西：
**把「齐不齐」变成系统能回答的问题**。

**初步设计（待需求会确认）**：

- **退税批次**：一票出口（或一张报关单）为一个批次，关联合同、出运单、采购订单
- **单据清单**：每个批次一张固定的必备单据表（报关单/进项发票/合同/运单/委托协议/……），
  每项有「已上传 / 缺失 / 不适用」三态，附件走已有的 S3 通道
- **进项发票配对**：出口的货是从哪几张采购单来的 → 那几张供应商发票就是进项来源。
  三单核对已经把这条链打通了，退税只是换个角度读它
- **状态流**：备案 → 收单 → 齐套 → 申报 → 已退。齐套之前不许申报（同 A3 的闸门思路）
- **台账与提醒**：申报期限倒计时；久未齐套的批次上工作台

**要问业务的**：退税率从哪来（商品编码对应，还是人工填）、申报周期与期限、
退税额怎么核对到账、单证不齐时的常见处置。

---

## 二点七、业务直接提的（E 组）——2026-08-23 立项

> 来源不是流程图也不是代码核验，是业务口述的三个真实痛点。核验后发现
> **地基比预想的厚**：三个都不是从零开始，缺的是最后一环。

| # | 内容 | 已有的地基 | 缺的一环 | 状态 |
|---|---|---|---|---|
| E1 | ~~**应收账款到期提醒**~~ **已完成（2026-08-23）**：到期日落库、催收清单页、每日提醒 worker、站内信铃铛 | — | — | ✅ |
| E2 | **提单签发提醒（部门级）**：开船后要盯着及时签提单，提醒要发给整个船务部门 | 船期有完整提醒机制（到港前 N 天 + 通知铃铛 + 工作台）、`etd`、单证类别已含 `BILL_OF_LADING_DRAFT/FINAL` | 两样：一条「开船后 N 天没上传提单正本」的规则；**按部门/角色群发**的收件人模式（现有提醒都发给负责人个人） | ⬜ |
| E3 | **客户与供应商信用评级 A/B/C/D** | 客户主数据已有 `credit_limit_minor`、`credit_currency`、`credit_status`、`payment_days` | 评级字段 + 评级变更历史；供应商侧整套都缺（现在只有 `payment_term`） | ⬜ |
| E4 | **货轮到港自动追踪** | 航线节点模型完备：多段航线、原始/最新 ETA、实际到离港、延误事件、到港提醒 | **数据源**。ETA 全靠船务手填，没有任何承运人 API / AIS / 第三方接入 | ⬜ |

### E1 应收账款到期提醒——细节

**到期日从哪来**：业务已定——**账期从合同生效日起算**（收到定金/预付款/
信用证之后才去下采购单，所以合同生效本身就意味着首付到位，之后的 120/150
天算的是尾款）。`due_date = 合同生效日 + 客户 payment_days`，**在签署那一刻
算好落库**，不是每次现算：客户改账期不该动已经发生的账——今天把账期从 120
调到 90，半年前那批合同不该「突然逾期」。

**第一期已完成（2026-08-23）**：`contracts.receivable_due_date` 落库（签署
事务内写，只写一次）、客户没配账期时**留空而不是当天到期**、催收清单页
（逾期红行 + 还剩几天 + 三个指标 + 逾期/未配账期两个筛子）、按销售负责人
围栏、存量补算 `BackfillReceivableDue`。集成测试九项判断全钉住，顺带把
`EXPORT_TEST_DSN` 接进 CI——export 此前一个 DB 测试都没跑过。

**第二期已完成（同日）**：`receivable_reminders` + 每日 worker + 站内信铃铛。
四档时机（30 天内 SOON / 7 天内 DUE / 逾期按周 OVERDUE）**用范围而非等号
判断**——写成「今天正好是到期前 30 天」的话，worker 那天没跑就永远错过；
范围加唯一键则是停几天再起来能一次补齐且不重复。跨租户扫描，新租户不需
要额外配置。铃铛按人隔离，逾期红点/临期橙点/其余灰点。

**顺带修的**：超管的数据范围一直是空的（引导只授权限、没配范围，而
export 的种子迁移跑在引导之前），管理员打开合同/收款/应收全是空的而
权限页正常——bootstrap 补上 + 迁移 00043 订正已有租户 + 测试钉住。
`mail` 有意不给：让超管看别人的邮箱该是显式决定。

**提醒怎么发**：复用船期那套现成的 worker（定时轮询 + 幂等 + 通知 + 工作台），
不新建一套。默认到期前 30/7/0 天各提醒一次，逾期后每 7 天一次。

**为什么它排第一**：这是三个里唯一**直接对应真金白银**的——逾期应收收不回来是
坏账，而现在系统能算出欠款却答不出「什么时候该收」。而且它是 E3 的前提：
没有逾期数据，信用评级只能拍脑袋。

### E2 提单签发提醒——已完成（2026-08-23）

`shipping_bl_reminders` + 每日 worker + 船形铃铛。触发：船开了（`atd`，
没登记则退回 `etd`）、满 3 天、且没有有效的 `BILL_OF_LADING_FINAL`——
**草稿不算**，草稿不是能拿去交单的东西。之后每 3 天一轮，直到正本上传。

**收件人是一群人，但表里一人一行**：部门是收件人的来源，不是收件人本身。
展开成多行之后「谁读了、谁还没读」才是可回答的问题；发一条挂在部门名下，
结果是谁都以为别人会处理。收件人 = 物流角色（`LOGISTICS`）全员 + 每张
船期自己的负责人——后者保证即使角色没配好，经办人也收得到。

没有复用 `shipping_arrival_reminders`：那张表和「到港」绑得很紧（外键指向
目的港节点、worker 里 JOIN 出到港文案、状态机为发邮件而设）。改用 E1 刚
验证过的模式——一条 SQL 扫描 + `INSERT ON CONFLICT`，幂等交给唯一键。
铃铛按催的轮次升级颜色：三轮以内橙，超过三轮（约十天）转红。

### E3 ✅ 信用评级第一期（2026-08-23 完成）

**别做成死字段**。人工填一个 A/B/C/D 下拉最省事，但会变成「填了没人更新」。
有用的做法是**系统给建议、人工定级**：系统算出「平均逾期 23 天、最大欠款 12 万美元、
近一年 8 笔全部按时」，评级人看着数据打分，评级连同依据一起存历史。

**第一期做的是人工那一半**（原计划就是这么分的）：

- **评级本身**：A/B/C/D，客户和供应商各一套，共用一张 `credit_ratings` 表——
  问的问题不同，但「谁在什么时候依据什么评了什么」是同一个形状
- **依据必填**。说不出为什么的评级等于没评，这是整张表存在的理由
- **每改一次留一行**，上一档记在同一行里——「从 A 降到 C」要一眼看得出，
  页面上降级用红点标出来
- **「多久没评了」**摆在评级旁边。一个三年没动过的 A，看着和上周刚判的 A
  一模一样，而它们说的完全不是一回事。超过半年自己变黄
- **依据数字留了位置**（`evidence` JSONB）。现在大多是空的，页面上明说
  「暂无数据」而不是显示 0——一个凭空的 0 看着像结论

**客户看什么**：逾期笔数、最长逾期、当前欠款。
**供应商看什么**（业务定的）：交期准不准、供货足不足量、质量合格率——
都是「看货」的维度，不含报价响应速度。三者在系统里都有现成的数据模型
（采购单的要求到货日、A5 的差额记录、A3 的质检记录），只是还没有数据。

**没做的**：系统给建议。生产上合同 0 张、采购单 0 条，算出来只会是一排 0。
等数据攒起来，往 `evidence` 里填就是，页面和历史都不用改。

> 踩到过一次：`credit_grade` 最初用 `CHAR(1)`，PostgreSQL 会把空串补成一个
> 空格，于是「没评过」读出来是一个看不见的空格——判空失败，按钮写着「重新
> 评级」而那家客户一次都没评过。改成 `VARCHAR(1)`。

### E4 ⬜ 货轮到港自动追踪

**核验结论：数据模型已经完备，缺的只是数据源。** `shipping_route_nodes`
支持整条航线——起运港 / 中转港 / **临时挂靠** / 目的港，每个节点都有
**原始 ETA**、**最新 ETA**、实际到港、原始 ETD、最新 ETD、实际离港，
节点状态走 计划 → 临近 → 已到 → 已离（可标记跳过）。主表有
ETD/ATD/ETA/ATA 与七种状态，另有 `shipping_delay_events` 延误记录、
可配规则的到港提醒、以及完整的变更留痕。

`original_eta_at` 与 `latest_eta_at` **分开存**这个设计本身就说明问题：
它天生是为「船公司改了 ETA、要看出改了多少」准备的。地基是按自动追踪
的样子打的，只是没接上源。

**但更新全是人工**：`UpdateRouteNodeTimes` / `SetDestinationETA` 都带
`updated_by`——船务的人从船司网站或邮件查到最新 ETA 再手填。搜遍
shipping 服务，**没有一行承运人 API、AIS 或第三方追踪的代码**。

| | 方案 | 成本 | 拿到什么 | 前提 |
|---|---|---|---|---|
| **甲** | **解析船公司邮件通知**（推荐先做）。船司本来就发 ETA 变更、到港、可提货通知到我们邮箱，而邮件系统 + AI 提取（C 组）已经在跑 | **零外部费用**，只有模型调用的几分钱 | 提单号 + 新 ETA → 自动更新 `latest_eta_at`，**人确认后生效**（照 C 组「AI 出草稿，人确认」）。到港通知还能顺带触发 ATA 与 E2 的提单提醒 | 无 |
| 乙 | **第三方集装箱追踪 API**（Terminal49 / Vizion / Portcast 这类），一个接口覆盖所有船司 | 业内按柜计费或订阅制，**量级需询价**——月柜量决定值不值 | 船司系统里的真实事件：装船 / 离港 / 转船 / 到港 / 卸货 / 可提货，最准最结构化 | **系统现在没有集装箱号字段**，要先补 |
| 丙 | **AIS 船舶位置**（MarineTraffic 等） | 最低（几百美元/年档） | 只有船的经纬度 | 无 |

**丙基本可以排除**：它追的是**船**不是**你的货**。船到港 ≠ 你的柜卸下来了
≠ 你能提货。适合大屏可视化，不适合驱动业务。

**建议先做甲**：零外部成本、复用已有的邮件 + AI 地基、覆盖面最广，
解决的正是现在最费人的那段（船务盯着邮箱手改 ETA）。做完若发现邮件
不够及时或不够全，再上乙。

**待业务确认**：① 主要用哪几家船公司（集中在两三家的话甲的邮件格式好
收敛）；② 一个月多少个柜（决定乙值不值）；③ 真正想知道的是「船到哪了」、
「什么时候到港」还是「我的柜什么时候能提货」——第三个业务价值最高，
但也最依赖乙。

**工作量**：甲中 · **依赖**：甲无（C 组的提取件可复用）；乙需先补集装箱号
· **来源**：2026-08-23 业务提问后核验

### E5 ⬜ 单点登录（SSO）

**核验结论（2026-08-24）：接进来的口子很干净，难的不是登录本身。**

已经有的：

- 登录成功后发的是一张 JWT（`pkg/authtoken` 的 `Issue`，带租户、员工号、
  姓名、邮箱）。**SSO 要替换的只是「怎么证明是本人」这一步，证完照样发同
  一张票** —— 网关和所有服务一行都不用动。这是最好的消息：接入点是一个
  函数，不是一层。
- OAuth 那套跳转-回调-验票的流程**我们写过一次**
  （`services/mail/internal/app/oauth.go`，连 Gmail 邮箱用的）。不是新领域。
- 已经有邀请/激活流程（`email_verified_at`）和租户域名校验
  （`IsTenantDomain`）。

缺的：

- `employees` 表**没有任何外部身份字段**，没地方记「这个人是企业微信里的谁」。
- 现在靠邮箱定位账号（`GetUserByEmail`），邮箱就是身份。

#### 值钱的是第二条，不是第一条

1. 少记一个密码 —— 300 人推广时阻力小很多。
2. **离职能一键关掉** —— 今天有人离职，要有人**记得**去 ERP 里停用他；忘了，
   账号就一直活着。接了 SSO，身份中心里一停，所有系统同时进不去。这是安全
   上的实质改善，不是便利性。
3. 我们不再保管密码，少一处可泄露的东西。

#### 三个必须先定的问题

| 问题 | 为什么卡人 |
|---|---|
| **接哪家** | 企业微信 / 飞书 / 钉钉 / Microsoft 365。取决于公司本来在用哪个 —— 员工没登着的那家，SSO 等于多一道手续，不是少一道 |
| **怎么对上员工** | 按邮箱对最自然，但**身份中心里的邮箱和 ERP 里的可能不是同一个**。对不上的人会被挡在门外，而且往往是在他最急的时候 |
| **第一次登录** | 身份中心有、ERP 里没有的人：自动建账号还是拒绝？自动建就绕过了现在的邀请流程和 `IsTenantDomain` 校验 |

#### 本地后门是必须的，不是可选的

身份中心挂了，全公司进不去 ERP。所以必须保留**至少一个本地密码账号**：

- 它不走 SSO
- 现有的账号锁定与网关限流对它照旧有效 —— 那套东西不能因为接了 SSO 就拆
- 平时不用，但要定期确认它还登得进去。**没验证过的后门等于没有后门**

#### 不做双因子的话，SSO 反而更安全

现在 ERP 没有双因子（搜遍 iam 服务，TOTP/短信一个都没有）。接了 SSO 之后，
双因子由身份中心提供 —— 企业微信、飞书都有。**这是接 SSO 顺带拿到的东西**，
比我们自己实现一套划算得多。

**工作量**：中 · **依赖**：先定接哪家（业务决定，不是技术决定）
· **来源**：2026-08-24 讨论邮箱登录方式时提出

---

## 三、建议顺序与理由

**A 组与 B 组已于 2026-08-22 全部完成**（原顺序 A1+B2 → A2 → A3→A4 → B1 →
B4+B5 → B3，实际按此走完）。以下是 C/D/E 三组的建议顺序：

**E1、E2、A5 已于 2026-08-23 完成**（见上文各节）。余下顺序：

1. ~~**E1 应收账款到期提醒**~~ —— 已完成。
2. ~~**A5**~~ —— 已完成，且比原计划多做了一层：不只是「货没到齐能关单」，
   还包括「一开始就知道只供 80 吨」时的分单、补购与换供应商。仍待业务回答
   的只剩溢短装口径与出现频率，以及「货到了但整单要推翻」是否真实存在。
3. ~~**E2 提单签发提醒**~~ —— 已完成。
4. ~~**D2 合同执行进程一览表**~~ —— 已完成。
5. **E4 货轮到港追踪（甲：邮件解析）** —— 紧跟 E2，因为两件事同一条动线：
   船司的到港通知既能更新 ETA，也正好是提单该签发的信号。而且它是 C 组
   AI 提取的第二个落点，能顺带验证那套件的通用性。
6. **C1 工厂报价解析（AI 第一枪）** —— 采购员日常最痛的手敲活。守卫已就位
   （报价完整性校验 + 比价页人审），模型错了会被拦住。先做这一个验证价值，
   再决定要不要继续 C2–C5。
6.5 **C6 智能路由 + 限量第二期** —— 排在 C1 之后不是因为不重要，是因为
   **现在还没有需要它的证据**：Luna 的提取效果够不够，要等真实用量攒够
   才知道；C1 落地后才有第二个花钱的地方，公共件的形状那时才看得清。
   真要动手，第一步（单价按模型分）可以单独先做——它不依赖任何业务决定，
   而且不做它，路由一上线钱就是错的。
7. ~~**E3 信用评级**~~ —— 第一期（人工评级 + 变更历史）已完成；系统建议
   那一半等数据积累。
8. **D1 出口退税** —— 值钱但要先开需求会（见上方要问业务的清单）。
   工作量最大的一块，别在需求没定之前动手。
9. **D3 信用证** —— 卡在一个业务问题上：公司到底有没有 L/C 结算。
   全 T/T 则永久搁置。
9.5 **E5 单点登录** —— 位置不固定，取决于一个业务决定：公司在用企业微信、
   飞书还是别的。定了就能做，**它不等数据**，这一点和 C1/E4/C6 都不同。
   如果 300 人铺开的日程排在前面，它应该跟着往前提 —— 因为「300 个人各自
   记一个新密码」和「离职忘了关账号」这两件事，都是在铺开那一刻才开始疼的。
10. **D4 单证制作 + 统计分析** —— 最后做。报表要等数据积累，空库上建报表没意义。

---

## 四、本文件之外

- **基础数据清单的 88 条未完成项**只抽查了要害部分（船港与供应商关系、
  数据关系与历史规则）。要逐条核验是另一轮工作。
- **明确不做的**：船舶主数据（清单自己写着“确认是否建立”）、
  船期的合同与单证阶段、导入导出审计、并发编辑冲突提示——
  这些等前两档清空后再议。
- **不在本文件范围**：运维、CI、监控、部署。那些在
  [DEPLOY.md](./DEPLOY.md) 与 [MONITORING.md](./MONITORING.md)。
