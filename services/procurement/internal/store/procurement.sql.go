// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: procurement.sql

package store

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const cancelRequirement = `-- name: CancelRequirement :one
UPDATE purchase_requirements SET
    status        = 'CANCELLED',
    closed_reason = $2::text,
    updated_at    = now()
WHERE tenant_id = $1 AND id = $3 AND status = 'PENDING'
RETURNING status
`

type CancelRequirementParams struct {
	TenantID int64
	Reason   string
	ID       int64
}

func (q *Queries) CancelRequirement(ctx context.Context, arg CancelRequirementParams) (string, error) {
	row := q.db.QueryRow(ctx, cancelRequirement, arg.TenantID, arg.Reason, arg.ID)
	var status string
	err := row.Scan(&status)
	return status, err
}

const closeCoveredRequirement = `-- name: CloseCoveredRequirement :execrows
UPDATE purchase_requirements SET
    status        = 'CANCELLED',
    closed_reason = $2::text,
    updated_at    = now()
WHERE tenant_id = $1
  AND contract_item_id = $3
  AND status = 'PENDING'
`

type CloseCoveredRequirementParams struct {
	TenantID       int64
	Reason         string
	ContractItemID int64
}

// Stock caught up with a line that used to be short. The requirement is not
// deleted — a buyer who saw it yesterday deserves to find out what became of
// it — and only untouched ones are closed: once something is on order, the
// goods are coming whether stock covered it in the meantime or not.
func (q *Queries) CloseCoveredRequirement(ctx context.Context, arg CloseCoveredRequirementParams) (int64, error) {
	result, err := q.db.Exec(ctx, closeCoveredRequirement, arg.TenantID, arg.Reason, arg.ContractItemID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const countOrderedOnOldVersions = `-- name: CountOrderedOnOldVersions :one
SELECT count(*) FROM purchase_requirements
WHERE tenant_id = $1
  AND contract_id = $2
  AND contract_version_id <> $3
  AND status IN ('PARTIALLY_ORDERED', 'ORDERED')
`

type CountOrderedOnOldVersionsParams struct {
	TenantID      int64
	ContractID    int64
	KeepVersionID int64
}

// Requirements from a superseded version that were already ordered. Not an
// error the consumer can fix, but a number a buyer has to be told about.
func (q *Queries) CountOrderedOnOldVersions(ctx context.Context, arg CountOrderedOnOldVersionsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countOrderedOnOldVersions, arg.TenantID, arg.ContractID, arg.KeepVersionID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createManualRequirement = `-- name: CreateManualRequirement :one
INSERT INTO purchase_requirements (
    tenant_id, contract_id, contract_no, contract_version_id, version_no,
    -- No contract line to key on, so the id is the key. Negative to stay out
    -- of the way of real contract_item_ids in the same unique index.
    contract_item_id, customer_name, product_id, sku_id, product_code,
    product_name, spec, uom_id, uom_code, required_qty, required_date,
    source, closed_reason, owner_id, owner_name
) VALUES (
    $1::bigint, 0, '', 0, 0,
    -nextval('purchase_requirements_id_seq'),
    '', $2::bigint,
    nullif($3::bigint, 0),
    $4::text,
    $5::text,
    $6::text,
    $7::bigint,
    $8::text,
    $9::text::numeric,
    nullif($10::text, '')::date,
    'MANUAL',
    $11::text,
    $12::bigint,
    $13::text
)
RETURNING id
`

type CreateManualRequirementParams struct {
	TenantID     int64
	ProductID    int64
	SkuID        int64
	ProductCode  string
	ProductName  string
	Spec         string
	UomID        int64
	UomCode      string
	RequiredQty  string
	RequiredDate string
	Remark       string
	OwnerID      int64
	OwnerName    string
}

// A buyer raising a requirement themselves: restocking, a long-lead item, a
// deal with a supplier. Deliberately not tied to a contract — purchasing is
// not only the tail end of a sale, and a company that can only buy what it
// has already sold cannot keep stock at all.
func (q *Queries) CreateManualRequirement(ctx context.Context, arg CreateManualRequirementParams) (int64, error) {
	row := q.db.QueryRow(ctx, createManualRequirement,
		arg.TenantID,
		arg.ProductID,
		arg.SkuID,
		arg.ProductCode,
		arg.ProductName,
		arg.Spec,
		arg.UomID,
		arg.UomCode,
		arg.RequiredQty,
		arg.RequiredDate,
		arg.Remark,
		arg.OwnerID,
		arg.OwnerName,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}

const getRequirement = `-- name: GetRequirement :one
SELECT
    id, contract_id, contract_no, contract_version_id, version_no,
    contract_item_id, customer_name, product_id,
    coalesce(sku_id, 0)::bigint AS sku_id,
    product_code, product_name, spec, uom_id, uom_code,
    required_qty::text AS required_qty,
    ordered_qty::text  AS ordered_qty,
    received_qty::text AS received_qty,
    coalesce(required_date::text, '')::text AS required_date,
    source, status, closed_reason, created_at,
    owner_id, owner_name
FROM purchase_requirements
WHERE tenant_id = $1 AND id = $2
`

type GetRequirementParams struct {
	TenantID int64
	ID       int64
}

type GetRequirementRow struct {
	ID                int64
	ContractID        int64
	ContractNo        string
	ContractVersionID int64
	VersionNo         int32
	ContractItemID    int64
	CustomerName      string
	ProductID         int64
	SkuID             int64
	ProductCode       string
	ProductName       string
	Spec              string
	UomID             int64
	UomCode           string
	RequiredQty       string
	OrderedQty        string
	ReceivedQty       string
	RequiredDate      string
	Source            string
	Status            string
	ClosedReason      string
	CreatedAt         pgtype.Timestamptz
	OwnerID           int64
	OwnerName         string
}

func (q *Queries) GetRequirement(ctx context.Context, arg GetRequirementParams) (GetRequirementRow, error) {
	row := q.db.QueryRow(ctx, getRequirement, arg.TenantID, arg.ID)
	var i GetRequirementRow
	err := row.Scan(
		&i.ID,
		&i.ContractID,
		&i.ContractNo,
		&i.ContractVersionID,
		&i.VersionNo,
		&i.ContractItemID,
		&i.CustomerName,
		&i.ProductID,
		&i.SkuID,
		&i.ProductCode,
		&i.ProductName,
		&i.Spec,
		&i.UomID,
		&i.UomCode,
		&i.RequiredQty,
		&i.OrderedQty,
		&i.ReceivedQty,
		&i.RequiredDate,
		&i.Source,
		&i.Status,
		&i.ClosedReason,
		&i.CreatedAt,
		&i.OwnerID,
		&i.OwnerName,
	)
	return i, err
}

const listRequirements = `-- name: ListRequirements :many
SELECT
    id, contract_id, contract_no, contract_version_id, version_no,
    contract_item_id, customer_name, product_id,
    coalesce(sku_id, 0)::bigint AS sku_id,
    product_code, product_name, spec, uom_id, uom_code,
    required_qty::text AS required_qty,
    ordered_qty::text  AS ordered_qty,
    received_qty::text AS received_qty,
    coalesce(required_date::text, '')::text AS required_date,
    source, status, closed_reason, created_at,
    owner_id, owner_name,
    count(*) OVER () AS total
FROM purchase_requirements
WHERE tenant_id = $1::bigint
  -- 数据范围（A1）：属主是合同负责人（手工需求是创建人）。owner_id=0 的
  -- 历史行只有 scope_all 能看见——fail-closed，错也只错在看不见。
  AND ($2::bool OR owner_id = ANY($3::bigint[]))
  AND ($4::text = '' OR status = $4::text)
  AND ($5::bigint = 0 OR contract_id = $5::bigint)
  AND ($6::text = ''
       OR contract_no ILIKE '%' || $6::text || '%'
       OR product_name ILIKE '%' || $6::text || '%'
       OR product_code ILIKE '%' || $6::text || '%')
ORDER BY
    -- Outstanding work first, then by when it is needed. A requirement with
    -- no date sorts last rather than first, which is what NULLS LAST buys.
    CASE WHEN status = 'PENDING' THEN 0 WHEN status = 'PARTIALLY_ORDERED' THEN 1 ELSE 2 END,
    required_date NULLS LAST,
    id DESC
LIMIT $8::int OFFSET $7::int
`

type ListRequirementsParams struct {
	TenantID   int64
	ScopeAll   bool
	OwnerIds   []int64
	Status     string
	ContractID int64
	Keyword    string
	RowOffset  int32
	RowLimit   int32
}

type ListRequirementsRow struct {
	ID                int64
	ContractID        int64
	ContractNo        string
	ContractVersionID int64
	VersionNo         int32
	ContractItemID    int64
	CustomerName      string
	ProductID         int64
	SkuID             int64
	ProductCode       string
	ProductName       string
	Spec              string
	UomID             int64
	UomCode           string
	RequiredQty       string
	OrderedQty        string
	ReceivedQty       string
	RequiredDate      string
	Source            string
	Status            string
	ClosedReason      string
	CreatedAt         pgtype.Timestamptz
	OwnerID           int64
	OwnerName         string
	Total             int64
}

func (q *Queries) ListRequirements(ctx context.Context, arg ListRequirementsParams) ([]ListRequirementsRow, error) {
	rows, err := q.db.Query(ctx, listRequirements,
		arg.TenantID,
		arg.ScopeAll,
		arg.OwnerIds,
		arg.Status,
		arg.ContractID,
		arg.Keyword,
		arg.RowOffset,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListRequirementsRow
	for rows.Next() {
		var i ListRequirementsRow
		if err := rows.Scan(
			&i.ID,
			&i.ContractID,
			&i.ContractNo,
			&i.ContractVersionID,
			&i.VersionNo,
			&i.ContractItemID,
			&i.CustomerName,
			&i.ProductID,
			&i.SkuID,
			&i.ProductCode,
			&i.ProductName,
			&i.Spec,
			&i.UomID,
			&i.UomCode,
			&i.RequiredQty,
			&i.OrderedQty,
			&i.ReceivedQty,
			&i.RequiredDate,
			&i.Source,
			&i.Status,
			&i.ClosedReason,
			&i.CreatedAt,
			&i.OwnerID,
			&i.OwnerName,
			&i.Total,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const supersedeRequirementsBefore = `-- name: SupersedeRequirementsBefore :many
UPDATE purchase_requirements SET
    status        = 'SUPERSEDED',
    closed_reason = $2::text,
    updated_at    = now()
WHERE tenant_id = $1
  AND contract_id = $3
  AND contract_version_id <> $4
  AND status = 'PENDING'
RETURNING id, contract_item_id, product_name
`

type SupersedeRequirementsBeforeParams struct {
	TenantID      int64
	Reason        string
	ContractID    int64
	KeepVersionID int64
}

type SupersedeRequirementsBeforeRow struct {
	ID             int64
	ContractItemID int64
	ProductName    string
}

// A contract change produces a new version with new line ids, so the previous
// version's requirements no longer describe anything that is owed.
//
// Only untouched ones are retired. A requirement somebody has already ordered
// against is a real commitment to a supplier, and quietly cancelling it here
// would hide a problem that a person needs to resolve.
func (q *Queries) SupersedeRequirementsBefore(ctx context.Context, arg SupersedeRequirementsBeforeParams) ([]SupersedeRequirementsBeforeRow, error) {
	rows, err := q.db.Query(ctx, supersedeRequirementsBefore,
		arg.TenantID,
		arg.Reason,
		arg.ContractID,
		arg.KeepVersionID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SupersedeRequirementsBeforeRow
	for rows.Next() {
		var i SupersedeRequirementsBeforeRow
		if err := rows.Scan(&i.ID, &i.ContractItemID, &i.ProductName); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertRequirement = `-- name: UpsertRequirement :one

INSERT INTO purchase_requirements (
    tenant_id, contract_id, contract_no, contract_version_id, version_no,
    contract_item_id, customer_name, product_id, sku_id, product_code,
    product_name, spec, uom_id, uom_code, required_qty, required_date, source,
    owner_id, owner_name
) VALUES (
    $1::bigint,
    $2::bigint,
    $3::text,
    $4::bigint,
    $5::int,
    $6::bigint,
    $7::text,
    $8::bigint,
    nullif($9::bigint, 0),
    $10::text,
    $11::text,
    $12::text,
    $13::bigint,
    $14::text,
    $15::text::numeric,
    nullif($16::text, '')::date,
    'CONTRACT',
    $17::bigint,
    $18::text
)
ON CONFLICT (tenant_id, contract_item_id) DO UPDATE SET
    required_qty        = excluded.required_qty,
    required_date       = excluded.required_date,
    contract_no         = excluded.contract_no,
    contract_version_id = excluded.contract_version_id,
    version_no          = excluded.version_no,
    product_name        = excluded.product_name,
    -- 合同重发或换版时刷新属主：负责人转手后，新版本生效即改归属。
    -- 事件不带属主（0）则保留原值，别把已知的抹成未知。
    owner_id   = CASE WHEN excluded.owner_id <> 0 THEN excluded.owner_id
                      ELSE purchase_requirements.owner_id END,
    owner_name = CASE WHEN excluded.owner_id <> 0 THEN excluded.owner_name
                      ELSE purchase_requirements.owner_name END,
    -- A line that is short again is owed again. Without this a requirement
    -- retired by a contract change — or closed because stock briefly covered
    -- it — stays closed forever, and the shortage sits there with nobody
    -- buying anything. Only automatic closures are revived: once a buyer has
    -- ordered against it the row belongs to that order, and PENDING would
    -- invite a second purchase of the same goods.
    status = CASE
                 WHEN purchase_requirements.status IN ('SUPERSEDED', 'CANCELLED')
                      AND purchase_requirements.ordered_qty = 0
                 THEN 'PENDING' ELSE purchase_requirements.status
             END,
    closed_reason = CASE
                        WHEN purchase_requirements.status IN ('SUPERSEDED', 'CANCELLED')
                             AND purchase_requirements.ordered_qty = 0
                        THEN '' ELSE purchase_requirements.closed_reason
                    END,
    updated_at = now()
RETURNING id
`

type UpsertRequirementParams struct {
	TenantID          int64
	ContractID        int64
	ContractNo        string
	ContractVersionID int64
	VersionNo         int32
	ContractItemID    int64
	CustomerName      string
	ProductID         int64
	SkuID             int64
	ProductCode       string
	ProductName       string
	Spec              string
	UomID             int64
	UomCode           string
	RequiredQty       string
	RequiredDate      string
	OwnerID           int64
	OwnerName         string
}

// Money and quantities cross this boundary as text, same rule as export: Go
// holds decimal strings and never a float64. See pkg/money.
//
// Statements that mix casts with parameters use named arguments throughout;
// interleaving them with positional $n leaves sqlc unable to infer a type.
// Idempotent by contract line. A redelivered ContractEffective hits the
// conflict and refreshes the snapshot rather than creating a second
// requirement for the same thing.
//
// Quantity is refreshed too, because export re-publishes the whole line set
// for a version and the line is the authority on what was sold.
func (q *Queries) UpsertRequirement(ctx context.Context, arg UpsertRequirementParams) (int64, error) {
	row := q.db.QueryRow(ctx, upsertRequirement,
		arg.TenantID,
		arg.ContractID,
		arg.ContractNo,
		arg.ContractVersionID,
		arg.VersionNo,
		arg.ContractItemID,
		arg.CustomerName,
		arg.ProductID,
		arg.SkuID,
		arg.ProductCode,
		arg.ProductName,
		arg.Spec,
		arg.UomID,
		arg.UomCode,
		arg.RequiredQty,
		arg.RequiredDate,
		arg.OwnerID,
		arg.OwnerName,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}
